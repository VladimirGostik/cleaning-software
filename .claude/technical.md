<!-- inogile:context-version=3 -->
# CleanMaster — Technical map

Companion to `business.md`. Captures **relationships, flows, and the *why* behind non-obvious decisions** an LLM/contributor needs before writing code. Generic conventions live in `CLAUDE.md` (`## Architecture invariants`); this file is the relationship map + rationale, not an inventory.

## Project type

laravel-be + inertia-fe

## Stack snapshot

Laravel 13 / PHP 8.5 (Sail) · Inertia 3 + Vue 3 + TypeScript · PostgreSQL 18 · Spatie laravel-data / permission (teams=`tenant_id`) / activitylog / medialibrary / query-builder / typescript-transformer · Tailwind 4 + DaisyUI 5.

## Domains

### identity-tenancy

The spine every other domain hangs off. **Not** a `BelongsToTenant` model itself — it *defines* tenancy.

- **Core:** `App\Models\User` (global identity, UUID, `locale`, `is_active`, `subscription_plan` SubscriptionPlanEnum, `ownedTenants() HasMany`) + `App\Models\Tenant` (firma, UUID, VAT-payer flag, `is_vat_payer`, `vat_rate`, `iban`, **`swift_bic`** (new, banking identity, snapshots to invoice supplier_swift), `registration_info` (SK law D11, Obchodný zákonník §3a), `invoice_number_format`, `owner_id` FK to User ON DELETE CASCADE, `interface() HasOne`) + `App\Models\TenantMembership` (User × Tenant pivot, `is_active`, `joined_at`).
- **Satellites:**
  - `App\Models\TenantInterface` — 1:1 to Tenant (bigint PK, deliberately NOT UUID; settings never in URLs/DTOs). Stores `color` (TenantColorEnum cast) + `invoice_template` (InvoiceTemplateEnum cast, default 'classic') + **invoice prefill defaults (new):** `default_constant_symbol` (nullable), `default_payment_type` (PaymentTypeEnum cast, default 'transfer'), `default_currency` (CurrencyEnum cast, default 'EUR'), `default_rounding_mode` (RoundingModeEnum cast, default 'none'). Cascades on delete. Tenant→interface() HasOne.
  - `App\Http\Middleware\TenantContextMiddleware` — resolves active tenant from session `active_tenant_id` (or first active membership), binds `app('current_tenant_id')`, calls `PermissionRegistrar::setPermissionsTeamId($tenantId)`. Without it the global scope and permission lookups have no tenant.
  - `App\Models\Role` extends `Spatie\Permission\Models\Role` + `LogsActivity` + `search` scope. Per-tenant via `tenant_id` team key.
  - `App\Http\Middleware\HandleInertiaRequests` — shares `tenant {active, available}` + `can {…}` + `tenantColors` (TenantColorEnum::options()) + `flash.justRegistered` (boolean) to every page.
  - `App\Http\Controllers\Auth\*` — login / logout / forgot-password / reset-password.
  - `App\Http\Controllers\Api\MeController` — returns `MeData` (userId, activeTenantId, permissions, features). Single `__invoke` endpoint, no `#[Authorize]` by design (self-endpoint, `auth` middleware is the gate).
  - `App\Data\Auth\MeData` DTO (`#[TypeScript]`): userId, activeTenantId (?string), permissions (list), features (list). Generated to `resources/js/types/generated.d.ts`.
  - `App\Http\Middleware\RequiresTenantFeature` and `routes/api.php` registered in `bootstrap/app.php` (api middleware group appends EncryptCookies → AddQueuedCookies → StartSession → LocaleMiddleware → TenantContextMiddleware → throttle:api).
- **Flow (login):** `POST /login` → `Auth\AuthController@login` (`LoginData` DTO) → session auth → `TenantContextMiddleware` binds tenant on subsequent requests → `/dashboard`.
- **Flow (capabilities):** FE navigates → `Inertia.router.on('before')` guard syncs store via `useAuthorization().ensureLoaded()` → `GET /api/me` (session-auth XHR) → returns MeData (permissions = active-tenant Spatie names, features = plan list, accountPlan = owner's SubscriptionPlanEnum value, remainingTenantSlots = int|null) → store updates → `allows(permission, feature)` AND-gate re-evaluates on each render.
- **Depends on:** nothing (root).
- **Depended on by:** **every** domain model via `tenant_id` FK + the per-tenant permission scope.
- **If you change Core, check:** `TenantContextMiddleware`, `HandleInertiaRequests::share`, `App\Scopes\TenantScope`, every seeder that calls `setPermissionsTeamId`, `MeController` (permissions + accountPlan + remainingTenantSlots hydration), `TenantFactory` (interface creation, owner assignment, invoice_number_format default, swift_bic + default_* nulls), Tenant model (owner relation, VAT/IBAN/registration_info/**swift_bic** fillable), TenantInterface (invoice_template/**default_*** fillable + casts), User model (subscription_plan cast, ownedTenants relation), RegistrationService::register (sets owner_id + invoice_number_format default), InvoiceService snapshots (when Tenant.swift_bic → supplier_swift, when TenantInterface defaults → prefill + issue snapshot), InvoiceSettingsService (Tenant.swift_bic + TenantInterface.default_* persist), InvoiceController::create/edit (prefill from TenantInterface defaults), RecurringInvoiceController::create/edit (prefill from TenantInterface defaults), `Settings/Invoicing.vue` form (swift_bic + default_* fields).
- **Keywords:** tenant, firma, membership, Vlastník, active_tenant_id, teams, /api/me, capabilities, tenantColors, justRegistered, swift_bic, invoice defaults (constant_symbol, payment_type, currency, rounding_mode), prefill, TenantInterface settings.

### subscription-plans

Per-account feature gating (entitlement, distinct from Spatie RBAC). Account's plan → tenant features + tenant-creation quota.

- **Core:** `config/subscription.php` — static matrix (4 tiers: Free/Starter/Pro/Enterprise); keyed by `SubscriptionPlanEnum` value; each plan has `max_tenants` (int|null), `features` array (FeatureEnum values), `quotas` map (feature → int|null). `App\Enums\SubscriptionPlanEnum` (Free|Starter|Pro|Enterprise) + `App\Enums\FeatureEnum` (Clients|Objects|Quotes|Contracts|Schedule|Invoices|Employees|Reports|MobileAccess|MultiUser). Both `#[TypeScript]`. `App\Contracts\ChecksFeatures` interface (DIP seam) + `App\Services\ConfigFeatureChecker` final impl.
- **Satellites:**
  - `App\Http\Middleware\RequiresTenantFeature` — `'feature'` alias in bootstrap/app.php. Usage: `Route::...->middleware('feature:clients')`. Resolves tenant from `app('current_tenant_id')`, loads owner, checks `$tenant->owner->subscription_plan` against `config/subscription.php`, aborts 403 (`app.feature.locked`) if plan lacks feature.
  - `App\Models\Tenant` — `hasFeature(FeatureEnum): bool` accessor (delegates to bound `ChecksFeatures::hasFeature($this, $feature)` which reads `$this->owner->subscription_plan`).
  - `App\Models\User` — `subscription_plan` cast to `SubscriptionPlanEnum` + `ownedTenants() HasMany` relation (default eager-loaded in `ConfigFeatureChecker::canCreateTenant`).
  - `ChecksFeatures::maxTenants(User): ?int` — returns the account's `max_tenants` from plan config. null = unlimited.
  - `ChecksFeatures::canCreateTenant(User): bool` — returns true if `User::ownedTenants()->count() < maxTenants(user)`.
  - `ChecksFeatures::featuresFor(Tenant): list<string>` — returns all feature enum values (as strings) for the tenant owner's plan. Used by FE capabilities store + `MeController`.
  - Translations `lang/{sk,en,uk}/app.php` — `subscription_plan.*` labels + `feature.locked` 403 message + `tenant.limit_reached` error.
- **Flow (feature check):** route-level `->middleware('feature:quotes')` → `RequiresTenantFeature` loads tenant + owner, checks owner's plan's feature list against `FeatureEnum::tryFrom($param)` → allow 200 or abort 403.
- **Flow (add-tenant quota):** `TenantController@store` calls `ChecksFeatures::canCreateTenant($user)` before creating new tenant. If false, aborts 403 with `app.tenant.limit_reached`.
- **Depends on:** `identity-tenancy` (reads `current_tenant_id` + User subscription_plan, Tenant owner relation).
- **Depended on by:** `identity-tenancy` (MeController calls `maxTenants` + `canCreateTenant` + `featuresFor`); every route gated `->middleware('feature:...')`; TenantController; FE capabilities layer.
- **If you change Core, check:** `config/subscription.php` (max_tenants matrix), `UserFactory` (default Free + states for other tiers), `UserSeeder` (demo account plans), `TenantFactory` (owner_id assignment), `TenantController@store` (canCreateTenant check), language files, any route gated `->middleware('feature:...')`, `MeController` (maxTenants + remainingTenantSlots hydration), `ConfigFeatureChecker::planConfig` (loads owner).
- **Keywords:** plan, feature, quota, entitlement, Free/Starter/Pro/Enterprise, gating, SubscriptionPlanEnum, FeatureEnum, ChecksFeatures, ConfigFeatureChecker, featuresFor.

### register-flow-fe

Web registration onboarding (3-step wizard → atomic backend transaction → auto-login + welcome overlay).

- **Core:** `Pages/Auth/Register.vue` 3-step machine (account → company → invites) + `RegistrationService::register(RegisterData)` on BE (atomic via `DB::transaction`). New User created with subscription_plan (default Free per spec), new Tenant created with owner_id = the new User, email auto-verified, user auto-logged-in, `active_tenant_id` set in session, Vlastník role assigned.
- **Satellites:**
  - `RegisterData` DTO (`#[TypeScript]`) — name, email, password, `company: CompanyData`, `invites: array<InviteData>`. CompanyData: name, ico, dic, vat_number, is_vat_payer, address_line, city, postal_code, country. InviteData: email, role_name.
  - `Pages/Auth/{RegisterHero, RegisterStepAccount, RegisterStepCompany, RegisterStepInvites, RegisterWelcome}.vue` — step components + hero banner (auth page palette: --auth-* not cleanmaster theme).
  - `Components/Forms/PasswordStrengthBar.vue` — visual strength indicator on account step.
  - `App\Http\Controllers\Auth\RegisterController` — `showRegister()` GET (renders page), `register()` POST with `RegisterData` DTO + throttle:register.
  - `RoleTemplatesSeeder` — refactored static `seedForTenant(Tenant $tenant)` method (called by `RegistrationService::bootstrapTenant()`, shared with add-tenant flow). Creates 6 spec roles + permissions per tenant.
  - `IcoLookupService` (BE) — synchronous lookup, currently hardcoded SKMap, `TODO swap for ARES API`.
  - `useIcoLookup()` composable (FE) — debounced 400ms lookup, calls `IcoLookupService.ts`, yields company name + VAT status to form.
  - `ColorSwatchPicker` component (company step) — choose 8 TenantColorEnum presets (hex palette), stored in TenantInterface.
  - Dashboard flash `justRegistered` (boolean) → `Pages/Dashboard.vue` renders welcome overlay (Option C: overlay on existing dashboard, not separate route/page).
  - `AppLayout` tenant dropdown now lists `tenant.available` + "Pridať novú firmu" link (→ AddTenantModal).
  - Rate limiter `register` added to `AppServiceProvider`.
- **Flow:** GET `/register` → RegisterController→showRegister() → Register.vue mounted, user enters 3 steps → POST `/register` (RegistrationService::register() transaction: User create with subscription_plan=Free, Tenant create with owner_id=the new User, TenantInterface create, TenantMembership create, seed roles, assign Vlastník, create invitations) → auto-login + redirect `/dashboard` with flash justRegistered=true → Dashboard overlay renders welcome → "Continue" dismisses overlay.
- **Depends on:** `identity-tenancy` (User subscription_plan field, Tenant owner_id FK, TenantMembership, RoleTemplatesSeeder), `subscription-plans` (new user defaults to Free plan).
- **Depended on by:** nothing (entry point).
- **If you change Core, check:** `RegisterController` route (throttle:register limiter), `RegistrationService::register` + `::bootstrapTenant` transaction boundary + owner_id assignment, `RoleTemplatesSeeder::seedForTenant` static signature, `TenantInterface` creation, Tenant model `owner() BelongsTo` relation, User model `subscription_plan` cast, `HandleInertiaRequests` (tenantColors shared prop, justRegistered flash), AppLayout tenant dropdown, Dashboard welcome logic.
- **Keywords:** register, wizard, onboarding, co-founder, invite, atomic, email_verified_at forceFill, TenantInterface, tenantColors, RoleTemplatesSeeder::seedForTenant static.

### ico-lookup

Guest-accessible synchronous company registry lookup (mock → TODO ARES API).

- **Core:** `GET /api/icos/{ico}` endpoint (guest-accessible, throttled, whereNumber validation) returns `IcoLookupData` DTO (ico, name, vat_status).
- **Satellites:**
  - `App\Http\Controllers\Api\IcoLookupController` — single action, throttle:ico-lookup.
  - `App\Services\IcoLookupService::lookup(string $ico): IcoLookupData` — current impl: hardcoded SKMap (demo IČOs map to company names + VAT status). Flags `TODO swap for ARES API` (Slovakia's official company registry) when user-facing.
  - `IcoLookupData` DTO (`#[TypeScript]`) — ico (string), name (?string), vat_status (?bool). Generated to resources/js/types/generated.d.ts.
  - `resources/js/services/IcoLookupService.ts` — async `lookup(ico): Promise<IcoLookupData>` over XHR.
  - `resources/js/composables/useIcoLookup.ts` — debounced 400ms wrapper, call `lookup()` as user types, yields name + VAT status. Cancellable on unmount.
  - Rate limiter `ico-lookup` added to `AppServiceProvider`.
  - FE integration: Register.vue CompanyStep uses `useIcoLookup` to populate name + vat_status on IČO input blur.
- **Flow (register company step):** User enters IČO → debounced 400ms → `useIcoLookup()` calls `IcoLookupService.lookup()` → `GET /api/icos/{ico}` (throttled, guest OK) → returns IcoLookupData → form name + vat_status fields auto-populate → user can override before POST /register.
- **Depends on:** nothing (stateless lookup service).
- **Depended on by:** `register-flow-fe` (company step auto-fill), `AddTenantModal` (optional).
- **If you change Core, check:** `IcoLookupController` route + throttle:ico-lookup limiter, `IcoLookupService::lookup` (when swapping mock for ARES), FE `useIcoLookup` debounce params (400ms), RegisterStepCompany form binding.
- **Keywords:** IČO, company lookup, registry, ARES TODO, mock, guest-accessible, throttled, debounced.

### clients

First implemented business domain layer (CRUD, filtering, soft-delete, multi-contact, type enum).

- **Core:** `App\Models\Client` (UUID, tenant_id FK, BelongsToTenant, SoftDeletes) + `App\Services\ClientService` — `paginate(ClientIndexFilterData): LengthAwarePaginator`, `create(ClientStoreData): Client`, `update(Client, ClientUpdateData): Client`, `delete(Client): void`.
- **Satellites:**
  - `App\Models\ClientContact` — N:1 to Client, FK `client_id ON DELETE CASCADE`. Holds `email`/`phone`/`is_primary`. **Client itself has no email/phone column** — both read from the primary contact.
  - `App\Data\Clients\*` — `ClientIndexFilterData` (filters: search, type), `ClientListItemData` (row + derived `primary_contact_email`/`primary_contact_phone`), `ClientDetailData` (address + contacts array, zero email/phone fields), `ClientStoreData`, `ClientUpdateData`, `ClientContactData`.
  - `App\Policies\ClientPolicy` — gates viewAny/view/create/update/delete on `view|create|edit|delete clients` permissions (references `PermissionEnum`).
  - `App\Enums\ClientTypeEnum` — `Corporate` (IČO required) | `Private` (IČO optional). `#[TypeScript]`. Methods: `label()` (i18n key), `options()` (select list).
  - FE: `Clients/Index` + `Clients/Show` Inertia pages, shared `ClientFormDrawer` (side-drawer create/edit, no separate routes), `useClientFilters` composable, generic `EmptyState` + `PageHeader`.
- **Flow (list):** `GET /clients` → `ClientController@index` (`#[Authorize('viewAny', Client)]`) → `ClientService::paginate` (Spatie QueryBuilder: filters search/type, sorts name/created_at) → `ClientListItemData::collect(..., PaginatedDataCollection)` → Inertia `Clients/Index`.
- **Flow (write):** `POST|PUT /clients` → `ClientController@store|update` (DTO) → `ClientService` in `DB::transaction` → `syncContacts` diff-apply (eager-load, diff incoming IDs, soft-delete missing, update/create matched) → redirect with `flash.success`.
- **Depends on:** `identity-tenancy` (tenant_id FK, permission gate).
- **Depended on by:** `objects` (Client HasMany CleaningObject via `client_id` FK). Quote → Contract → Invoice will also FK to Client (not yet built).
- **If you change Core, check:** `ClientController` (5 actions), `ClientPolicy`, `routes/web.php` (explicit routes: `clients.index`, `clients.show`, `clients.store`, `clients.update`, `clients.destroy`), `lang/{sk,en,uk}/app.php` `clients.*` keys, `objects` domain if Client model changes relation shape.
- **Keywords:** klient, kontakt, IČO, DIČ, IČ DPH, Corporate, Private, primary contact.

### objects

Physical cleaning locations (office/apartment/house/common areas), assigned to clients. Central entity in the chain client → object → (quote → contract → invoice). Holds access info and metadata.

- **Core:** `App\Models\CleaningObject` (PHP class name avoids `Object` global collision; table = `objects`). UUIDv7, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity. Direct `client_id` FK (restrictOnDelete; soft-cascade in ClientService::delete via explicit soft-delete call). Reassignable client_id via ObjectUpdateData. Fields: name, type (ObjectTypeEnum: office/apartment/house/common_areas), address (street/city/postal_code/country), access info (access_code/key_box_code/key_count), special_instructions, area_sqm, floor, is_active. GPS columns (gps_lat/gps_lng) reserved in migration for Phase 2, NOT exposed in DTOs. `App\Services\ObjectService` — paginate/create/update/delete, Spatie QueryBuilder filters (search/type/client_id/is_active).
- **Satellites:**
  - `App\Enums\ObjectTypeEnum` — Office | Apartment | House | CommonAreas. `#[TypeScript]`. Methods: `label()` (i18n key), `options()` (select list).
  - `App\Data\Objects\*` — `ObjectIndexFilterData` (filters: search, type, client_id, is_active, per_page), `ObjectListItemData` (row data), `ObjectDetailData` (full object + client relation), `ObjectStoreData` (client_id via `Rule::exists(...)->where('tenant_id', $tenantId)` — closes cross-tenant leak), `ObjectUpdateData` (incl client_id reassignment), `ObjectOptionData` (lightweight: id, name, client_id; passed to FE for invoice subject picking).
  - `App\Policies\ObjectPolicy` — gates viewAny/view/create/update/delete on `view|create|edit|delete objects` permissions (references `PermissionEnum`).
  - FE: `Objects/Index` + `Objects/Show` Inertia pages, shared `ObjectFormDrawer` (side-drawer create/edit, no separate routes), `useObjectFilters` composable (state mgmt for filter input), `ObjectTypeBadge` component.
  - Routes feature-gated `feature:objects` (plan axis): objects.index/show/store/update/destroy.
- **Flow (list):** `GET /objects` (middleware `feature:objects` checks plan) → `ObjectController@index` (`#[Authorize('viewAny', CleaningObject)]`) → `ObjectService::paginate` (Spatie QueryBuilder: filters search/type/client_id/is_active, sorts name/created_at, eager-loads client) → `ObjectListItemData::collect(...)` → Inertia `Objects/Index`.
- **Flow (write):** `POST|PUT /objects` (middleware `feature:objects`) → `ObjectController@store|update` (DTO) → `ObjectService` in `DB::transaction` → update + load client → `ObjectDetailData` render or redirect with `flash.success`.
- **Depends on:** `identity-tenancy` (tenant_id FK, permission gate), `subscription-plans` (route middleware `feature:objects` checks plan).
- **Depended on by:** Quote FK to Object (generates proposal). Contract may polymorphically bind to Object (future). Schedule/Job binds to Object. Invoice references Job (via Object).
- **If you change Core, check:** `ObjectController` (5 actions), `ObjectPolicy`, `routes/web.php` (feature:objects middleware + 5 named routes), `lang/{sk,en,uk}/app.php` `object_type.*` keys, `RoleTemplatesSeeder` (Sekretárka/Vedúca/Upratovačka/Účtovníčka object perms), Client::destroy soft-delete logic (if it cascade-deletes objects), `AppServiceProvider` explicit policy binding (CleaningObject → ObjectPolicy).
- **Keywords:** lokality, umiestnenie, prístupové kódy, pristup, office, apartment, house, common_areas, access_code, key_box_code, gps (reserved), client FK, central entity, feature:objects.

### quotes

Proposal / price quote workflow. Line items with per-row VAT rate + discount. Converts to invoice or contract.

- **Core:** `App\Models\Quote` (UUIDv7, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity, `client_id` FK, `cleaning_object_id` nullable FK) + `App\Models\QuoteItem` (UUIDv7, tenant_id FK, cascadeOnDelete via `quote_id`; columns: `description`, `quantity`, `unit_price`, **`discount_percent`** [0–100], **`vat_rate`**, **`line_base/line_vat/line_total`** [stored breakdown, computed at issue]). `App\Models\QuoteNumberSequence` — bigint PK, per-tenant per-year tracking. `App\Services\QuoteService` (final readonly, injects `NotificationRecipientResolver $resolver`) — `paginate(QuoteIndexFilterData)`, `create(QuoteUpsertData)`, `update(Quote, QuoteUpsertData)`, `delete(Quote)`, `send(Quote)` (status Draft→Sent + stamp `sent_at` + dispatches `QuoteSent` to `view quotes` holders), `accept(Quote)`, `reject(Quote)`, `duplicate(Quote)`, **`convertToInvoice(Quote): Invoice`** (delegates to InvoiceService::create, sets backlink `invoice->quote_id`), **`convertToContract(Quote): Contract`** (delegates to ContractService::create, sets backlink `contract->quote_id`). Lifecycle guards: `isEditable()` (Draft only), `canBeConverted()` (Accepted only). `App\Services\QuoteNumberService` — `next(Tenant, DateTimeInterface): string` (lockForUpdate sequence per tenant+year, format `CP{YYYY}{NNNN}`).
- **Satellites:**
  - `App\Enums\QuoteStatusEnum` — `#[TypeScript]`. Status: Draft|Sent|Accepted|Rejected|Expired, with `canTransitionTo()` method. `isEditable() = (this === Draft)`.
  - `App\Data\Quotes\{QuoteUpsertData, QuoteItemData, QuoteListItemData, QuoteDetailData, QuoteIndexFilterData}` — DTOs per spec. UpsertData validates `client_id` + optional `cleaning_object_id` tenant-scoped. ItemData: input `vat_rate`, `discount_percent`; output `line_base`, `line_vat`, `line_total` (computed at issue, immutable after). ListItemData: row data + derived `customer_display_name`. DetailData: full quote + items + VAT recap (reuses `VatBreakdownLineData`).
  - `App\Policies\QuotePolicy` — gates viewAny/view/create/update/delete/send/accept/reject/duplicate/convertToInvoice/convertToContract on `view|create|edit|send|approve|delete|convert_to_invoice|convert_to_contract quotes` permissions (scoped per action; convert actions also check InvoicePolicy/ContractPolicy).
  - `App\Http\Controllers\QuoteController` — thin, `#[Authorize]` per action. 14 routes: index/create/store/show/edit/update/destroy + POST send/accept/reject/duplicate/convert-invoice/convert-contract + GET pdf (behind `auth` + `feature:quotes`).
  - `App\Console\Commands\ExpireQuotes` — daily scheduled (routes/console.php). **Phase 1:** quotes where status ∈ [Draft, Sent] + valid_until < today → status Expired + structured log + dispatches `QuoteExpired` notification to `view quotes` holders via `NotificationRecipientResolver`. **Phase 2:** quotes with valid_until in exactly 7/3/1 days → `QuoteExpiring` notification. Injects `NotificationRecipientResolver` in `handle()`.
  - `App\Contracts\RendersQuotePdf` interface (DIP) — `render(Quote): Stream` seam for PDF generation. Bound in `AppServiceProvider`.
  - `App\Services\Pdf\QuotePdfService` — implements `RendersQuotePdf`. Uses `spatie/laravel-pdf` `chrome` driver (arm64 Chromium via Playwright). Single Blade template `resources/views/pdf/quotes/default.blade.php`, renders quote header, line items (with per-row discount + vat_rate), VAT recap (by rate), totals.
  - `App\Enums\PermissionEnum` — added `ViewQuotes`, `CreateQuotes`, `EditQuotes`, `SendQuotes`, `ApproveQuotes` (accept/reject), `DeleteQuotes`, `DuplicateQuotes`, `ConvertToInvoice`, `ConvertToContract`.
  - Routes feature-gated `feature:quotes` (Pro/Enterprise): `quotes.{index,create,store,show,edit,update,destroy,send,accept,reject,duplicate,pdf,convert-invoice,convert-contract}`.
  - FE: `Pages/Quotes/{Index,Create,Edit,Show}.vue` + components (`QuoteForm`, `QuoteItemsEditor` [per-row VAT-rate + discount cols], `QuoteSubjectPicker`, `QuoteStatusBadge`, `QuoteFilters`). Reuses `useInvoiceTotals` + `InvoiceVatRecap` (shared logic). Composables: `useQuoteFilters` (state mgmt). AppLayout nav: "Cenové ponuky" entry, gated `view quotes` + `feature:quotes`.
  - i18n keys: `quote_status.*`, `quotes.*` (list, detail, form, filters).
  - Tests: 5 files (QuoteServiceTest, QuotePolicyTest, ExpireQuotesCommandTest, QuoteConversionTest, QuoteControllerTest) — 50 tests covering service lifecycle, policy authorization, controller actions, conversion to invoice/contract, command scheduling, PDF rendering, state machine transitions.
- **Flow (create):** `GET /quotes/create` (middleware `feature:quotes`, `#[Authorize('create', Quote)]`) → props: clients, objects (opt, client-scoped FE), VAT rate options → `Pages/Quotes/Create.vue` → `QuoteForm` → POST `/quotes` (QuoteUpsertData) → `QuoteService::create` transaction: resolve Client/CleaningObject (tenant-scoped), compute VAT recap, persist → redirect `/quotes/{id}` with flash.
- **Flow (send):** `POST /quotes/{id}/send` → guard `isEditable()` → `QuoteService::send` → status Draft→Sent, stamp `sent_at` → `NotificationRecipientResolver::usersWithPermission(tenantId, ViewQuotes)` → dispatch `QuoteSent` notification → redirect with flash.
- **Flow (accept/reject):** `POST /quotes/{id}/accept|reject` → `QuoteService::accept|reject` → status Sent→Accepted|Rejected, stamp `accepted_at|rejected_at` → Accepted opens conversion actions.
- **Flow (convert):** `POST /quotes/{id}/convert-invoice` or `convert-contract` (guard `canBeConverted()` + policy checks on target) → `QuoteService::convertToInvoice|convertToContract` transaction: delegates to existing `InvoiceService::create|ContractService::create` with quote line items/SK fields mapped, sets backlink `invoice|contract.quote_id` → redirect to new resource with flash.
- **Flow (PDF):** `GET /quotes/{id}/pdf` → `#[Authorize('view', …)]` → `QuotePdfService::render` → `spatie/laravel-pdf` chrome driver → streamDownload.
- **Flow (expiry cron):** `app:expire-quotes` daily → Phase 1: `Quote::withoutGlobalScope(TenantScope)` where status ∈ [Draft, Sent] + valid_until < today → flip to Expired + `QuoteExpired` notification to `view quotes` holders. Phase 2: quotes with valid_until in exactly 7/3/1 days → `QuoteExpiring` notification.
- **Depends on:** `identity-tenancy` (tenant_id FK, permission gate), `subscription-plans` (route middleware `feature:quotes`), `clients` (Client FK, subject source), `objects` (CleaningObject optional FK, subject source).
- **Depended on by:** `invoices` (optional `quote_id` FK, backlink for audit trail when generated), `contracts` (optional `quote_id` FK, backlink for audit trail when generated).
- **If you change Core, check:** `QuoteController` (14 actions), `QuotePolicy`, `routes/web.php` (`feature:quotes` middleware + 14 named routes), `QuoteService` (convertToInvoice/convertToContract delegate + backlink assignment), `InvoiceService::create` (when quote_id passed, store it), `ContractService::create` (when quote_id passed, store it), `lang/{sk,en,uk}/app.php` `quote_status.*` + `quotes.*` keys, `RoleTemplatesSeeder` (Sekretárka/Vedúca/Účtovníčka quote perms), `Quote` model (client relation, cleaningObject relation, invoices/contracts HasMany relations), `AppServiceProvider` policy binding.
- **Keywords:** cenová ponuka, návrh, výpočet, line items, VAT rate, discount, status (draft/sent/accepted/rejected/expired), send, accept, reject, convert to invoice, convert to contract, quote_id backlink, expire cron, feature:quotes, Pro/Enterprise.

### invitation-accept

Token-based invitation acceptance (guest-accessible). Turns a Pending `TenantInvitation` into an active membership + role, with user upsert.

- **Core:** `App\Http\Controllers\InvitationController` (`show`/`accept` on `GET|POST /invitations/{token}`, guest-accessible) + `App\Services\InvitationAcceptService` — `resolve(string $token): TenantInvitation` (bypasses TenantScope via `withoutGlobalScope`), `accept(TenantInvitation, AcceptInvitationData, bool $skipPasswordCheck = false): User` in `DB::transaction`.
- **Satellites:**
  - `App\Data\Invitations\AcceptInvitationData` — DTO at controller boundary (password, ?name).
  - `resources/js/Pages/Invitations/Accept.vue` — single page rendering 4 states: `expired` / `wrong_user` (logged in with different email) / `existing_user` (password confirm) / `new_user` (name + password form).
  - `TenantInvitation::isAcceptable()` + `markAccepted()` — status guard (410 if not acceptable) + transition Pending→Accepted.
  - `tests/Feature/InvitationAcceptTest.php` — covers all 4 states + same-email auto-accept + membership reactivation.
- **Flow (accept):** `GET /invitations/{token}` → `resolve()` (no tenant scope — guest has no tenant context) → state branch: logged-in same-email → auto-accept (`skipPasswordCheck: true`); otherwise render Accept.vue → `POST /invitations/{token}` (`AcceptInvitationData`) → `accept()` transaction: existing user → `Hash::check` password (ValidationException on mismatch); new user → create (Free plan, `email_verified_at` forceFill) → `setPermissionsTeamId($invitation->tenant_id)` → membership create or `is_active` reactivate → assign role by `role_name` within tenant → `markAccepted()` → post-commit: `Auth::login` + session `active_tenant_id` + regenerate → `/dashboard` with flash.
- **Depends on:** `identity-tenancy` (TenantMembership, Role per-tenant team scope, session `active_tenant_id`), `subscription-plans` (new user defaults Free).
- **Depended on by:** `register-flow-fe` + add-tenant (both create the TenantInvitation rows this flow consumes).
- **If you change Core, check:** `routes/web.php` (invitations.show/accept, guest-accessible), `TenantInvitation::isAcceptable()` semantics (expiry 7d + Pending status), `RegistrationService::createInvitations` (dispatches `InvitationCreated` notification to invitee email via `Notification::route('mail', ...)`), `TenantController` invitation creation, partial unique index on (tenant_id,email) WHERE pending, `Accept.vue` state prop contract.
- **Keywords:** pozvánka, invitation, token, accept, prijať, membership reactivation, same-email auto-accept, user upsert, InvitationCreated notification.

### invoices

Invoicing domain (three subject modes, historical snapshot, SK compliance + VAT breakdown + deposit, PDF/QR, lifecycle actions).

- **Core:** `App\Models\Invoice` (UUID, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity, columns: subject mode + snapshot + VAT + SK fields + status/lifecycle) + `App\Models\InvoiceItem` (UUID, tenant_id FK, cascadeOnDelete; columns: `description`, `quantity`, `unit_price` [net], **`discount_percent`** [0–100], **`vat_rate`** [per-row], **`line_base/line_vat/line_total`** [stored breakdown]). `App\Services\InvoiceService` — `paginate(InvoiceIndexFilterData)`, `create(InvoiceUpsertData): Invoice`, `update(Invoice, InvoiceUpsertData)`, `issue(Invoice, InvoiceIssueData)` (Draft→Issued, auto or manual number, snapshot freeze), `markPaid(Invoice)` (Issued→Paid), `cancel(Invoice)` (Issued|Overdue→Cancelled + create credit-note with negated items/fields), `duplicate(Invoice)` (copy as Draft), **`syncItems`** (per-item VAT rate + discount + breakdown calc), **`computeTotals`** (VAT recap grouping, rounding, deposit handling). `App\Services\InvoiceNumberService` — `next(Tenant, DateTimeInterface): string` (lockForUpdate sequence per tenant+year, format via placeholders `{YYYY}/{YY}/{MM}/{X+}`), `variableSymbol(string): ?string` (digits of number, NULL if none).
- **Satellites:**
  - `App\Models\InvoiceNumberSequence` — bigint PK (operational, no UUID); `(tenant_id, year)` unique; tracks `last_number`.
  - `App\Enums\{InvoiceStatusEnum, InvoiceTypeEnum, InvoiceTemplateEnum, PaymentTypeEnum, CurrencyEnum, RoundingModeEnum}` — `#[TypeScript]`. Status: Draft|Issued|Paid|Overdue|Cancelled, with `canTransitionTo()` method. Type: Monthly|OneOff|Special. Template: Classic|Modern|Minimal (maps to Blade view path). **PaymentTypeEnum (new):** Transfer|Cash|Card|CashOnDelivery|Other. **CurrencyEnum (new):** EUR|CZK|USD (uppercase ISO 4217, deliberate deviation from lowercase-enum convention; QR payload + SK payment tooling expect uppercase; class PHPDoc explains rationale for reviewer-agent). **RoundingModeEnum (new):** None|Document|Cash005, with `round(float): float` instance method (none=as-is; document=round to whole €; cash005=nearest 0.05 €).
  - `App\Data\Invoices\{InvoiceUpsertData, InvoiceIssueData, InvoiceItemData, InvoiceListItemData, InvoiceDetailData, InvoiceSupplierData, InvoiceSettingsData, InvoiceIndexFilterData, VatBreakdownLineData}` — DTOs per spec D2–D4. Single UpsertData for store+update (shapes identical). Single tenant-scoped `client_id`/`cleaning_object_id` exists check + "object must belong to client" closure rule. **InvoiceItemData (updated):** input `vat_rate`, `discount_percent` (0–100); output `line_base`, `line_vat`, `line_total` (computed once at issue, immutable after). **InvoiceUpsertData (updated):** added `float $deposit` (default 0), **SK-standard fields:** `?string $constant_symbol`, `?string $specific_symbol`, `PaymentTypeEnum $payment_type`, `CurrencyEnum $currency`, `RoundingModeEnum $rounding_mode`, `?string $header_text`, `?string $footer_text`, **added** `?string $quote_id` (optional backlink to source Quote). **InvoiceDetailData (updated):** added `string $deposit`, `string $balance_due` (accessor, not stored), `array $vat_breakdown` (collection of `VatBreakdownLineData`). **VatBreakdownLineData (new):** `float $rate`, `float $base`, `float $vat`, `float $total` — one entry per VAT rate in the invoice, calculated once in `computeTotals()`. **InvoiceSupplierData (updated):** added `?string $swift` (snapshot of Tenant.swift_bic at issue). **InvoiceSettingsData (updated):** added `?string $swift_bic` on Tenant, `?string $default_constant_symbol`, `PaymentTypeEnum $default_payment_type`, `CurrencyEnum $default_currency`, `RoundingModeEnum $default_rounding_mode` on TenantInterface.
  - `App\Data\Objects\ObjectOptionData` — lightweight DTO (`#[TypeScript]`) used by `InvoiceController::create()`/`edit()` to pass all tenant objects to FE. Fields: id, name, client_id. FE filters client-side for object dropdown.
  - `App\Policies\InvoicePolicy` — gates viewAny/view → `ViewInvoices`; create → `CreateInvoices`; update/issue/markPaid → `EditInvoices`; cancel/delete (Draft only) → `CancelInvoices`.
  - `App\Http\Controllers\{InvoiceController, InvoiceSettingsController}` — thin, `#[Authorize]` per action. Index/Create/Show/Edit/Destroy + invokable-style issue/pay/cancel/duplicate (POST, redirect with flash). Create/Edit actions return `objects: ObjectOptionData[]`, `vatRateOptions` (SK: 23/19/5/0%), `paymentTypeOptions`, `currencyOptions`, `roundingModeOptions`, `invoiceDefaults` (prefilled from TenantInterface) props. Settings `show`/`update` → `InvoiceSettingsService` transaction (persists Tenant.swift_bic + TenantInterface defaults).
  - `App\Services\InvoiceSettingsService` — updates `Tenant` (invoice_number_format, iban, vat_rate, registration_info) + `TenantInterface.invoice_template` in transaction.
  - `App\Contracts\RendersInvoicePdf` interface (DIP) — `render(Invoice, TemplateEnum): Stream` seam for PDF generation. Bound in `AppServiceProvider`.
  - `App\Services\Pdf\InvoicePdfService` — implements `RendersInvoicePdf`. Uses `spatie/laravel-pdf` with the `chrome` driver (`chrome-php/chrome`, pure-PHP DevTools — no Node at runtime) to render Blade template per invoice template enum, returns PDF string. Blade views in `resources/views/pdf/invoices/{classic,modern,minimal}.blade.php`. Requires real arm64 Chromium in Docker (`docker/8.5/Dockerfile` installs it via Playwright, stable symlink `/usr/local/bin/chromium-real`; `LARAVEL_PDF_DRIVER=chrome` + `CHROMIUM_PATH=/usr/local/bin/chromium-real` in `.env`). Tests mock the interface to avoid Chrome dependency in CI.
  - `App\Services\Pdf\PayBySquareService` — generates Pay-by-Square QR code (IBAN+amount+VS+due_date) as data-URI via `engazan/pay-by-square` + `bacon/bacon-qr-code`. **Updated:** amount = `invoice->balance_due` (not total; deposit reduces it). Currency guard: returns `null` if `invoice->currency !== EUR` (Pay-by-Square SK EUR-domestic standard). **Updated:** optionally sets BIC from `invoice->supplier_swift` via `setSwift()`. Injected into Blade template as prop; when null, QR not rendered.
  - `App\Notifications\InvoiceIssued` — mail-only notification (`#[Tries(3)]`, `#[Backoff([10,30,60])]`, `#[Timeout(120)]`, `afterCommit()`). `via()` returns `['mail']`. Loads Invoice via `withTrashed()->findOrFail`, calls `RendersInvoicePdf::render()`, attaches PDF to `MailMessage`. `failed()` logs invoice_id + exception. Replaced the deleted `App\Jobs\SendInvoiceEmail` job + `App\Mail\InvoiceIssuedMail` mailable.
  - `App\Listeners\StampInvoiceSentAt` — handles `NotificationSent` (framework event), stamps `Invoice.sent_at` when `$event->notification instanceof InvoiceIssued`. Registered in `AppServiceProvider` via `Event::listen`.
  - `App\Console\Commands\MarkOverdueInvoices` — daily scheduled, flips Issued past due_date → Overdue (idempotent, scope-bypassed via withoutGlobalScope(TenantScope)).
  - `TenantInterface` new column: `invoice_template` (InvoiceTemplateEnum cast, default 'classic').
  - `Tenant` new column: `registration_info` (nullable string, SK accounting zápis v registri per D11).
  - Routes feature-gated `feature:invoices` (Pro/Enterprise): `invoices.{index,create,store,show,edit,update,destroy,issue,pay,cancel,duplicate}` + `settings.invoicing` (GET/PUT, feature-gated).
  - FE: `Pages/Invoices/{Index,Create,Edit,Show}.vue` + `Components/Invoices/{InvoiceForm, InvoiceItemsEditor, InvoiceStatusBadge, InvoiceSubjectPicker, InvoiceVatRecap}.vue` (new) + `Pages/Settings/Invoicing.vue` + `useInvoiceFilters.ts` + **`useInvoiceTotals.ts`** (new, per-item VAT + discount + recap + balance calc) composables. **Updated:** InvoiceItemsEditor now has per-row VAT-rate select + discount % input; InvoiceForm has deposit field; Show renders `<InvoiceVatRecap>` + deposit/balance_due rows + SK fields (KS/ŠS/payment_type/currency/SWIFT/header/footer/rounding); Settings page has swift_bic + invoice defaults fields (KS/payment/currency/rounding).
  - i18n keys: `invoices.*`, `invoice_type.*`, `invoice_status.*`, `invoice_template.*`, `invoices.items.{discount, vat_rate}`, `invoices.detail.{constant_symbol, specific_symbol, swift, currency, payment_type, rounding, header_text, footer_text, deposit, balance_due}`, `invoices.vat_recap.*`, `invoices.pdf.*` + per-rate recap rows, `payment_type.*`, `currency.*`, `rounding_mode.*`, `invoice_settings.{swift_bic, default_*}`.
  - Docker: `docker/8.5/Dockerfile` installs real arm64 Chromium via Playwright (`/usr/local/bin/chromium-real`). Rebuild on first clone: `docker compose build --no-cache laravel.test`.
- **Flow (create):** `GET /invoices/create` (middleware `feature:invoices`, `#[Authorize('create', Invoice)]`) → props: clients list, objects list, enum options (VAT rates, payment types, currencies, rounding modes), tenant defaults (swift_bic, invoice defaults) → `Pages/Invoices/Create.vue` → `InvoiceForm` (prefills from defaults) → POST `/invoices` (InvoiceUpsertData: item vat_rate/discount, invoice deposit/SK fields) → `InvoiceService::create` transaction: resolve Client/CleaningObject (tenant-scoped), snapshot customer/object/supplier blocks (+ supplier_swift from Tenant.swift_bic), **syncItems** (per-item VAT rate + discount → line_base/vat/total), **computeTotals** (VAT recap per rate, rounding applied, balance_due calc), persist → redirect `/invoices/{id}` with flash.
- **Flow (issue):** `POST /invoices/{id}/issue` (InvoiceIssueData: optional number override) → `InvoiceService::issue` transaction: manual number → validate tenant-scoped uniqueness, sequence untouched; else auto-assign via `InvoiceNumberService::next`; set `issued_at`, status Issued, `variable_symbol` (digits), `is_vat_payer`/`vat_rate`/rounding/SK-fields snapshots (frozen for PDF), render PDF in view prep → Show page displays QR (if EUR + balance_due > 0) + issue_date/delivery_date/due_date + VAT recap + deposit/balance_due + SK fields (KS/ŠS/payment/currency/SWIFT/header/footer/rounding).
- **Flow (PDF):** `GET /invoices/{id}/pdf` → `#[Authorize('view', …)]` → `InvoicePdfService::render` (Blade view per `$invoice->template->view()`, data = invoice + items [with line_base/vat_rate/discount per row] + VAT recap + SK fields [payment_type/constant_symbol/specific_symbol/supplier_swift/currency/rounding] + QR data-URI from `PayBySquareService` [null if non-EUR]) → `spatie/laravel-pdf` chrome driver → streamDownload (filename: number or 'draft').
- **Flow (email):** `POST /invoices/{id}/send` → `InvoiceController` guard Issued + customer_email → `Notification::route('mail', $customer_email)->notify(new InvoiceIssued($invoiceId))` (queued, afterCommit) → job loads Invoice, calls `RendersInvoicePdf::render()`, attaches PDF to `MailMessage` → on delivery: `NotificationSent` event → `StampInvoiceSentAt::handle()` stamps `Invoice.sent_at`.
- **Flow (cancel):** `POST /invoices/{id}/cancel` → guard `canBeCancelled()` → `InvoiceService::cancel` transaction: status Cancelled, `cancelled_at`, create credit-note invoice (replicate, negate item totals, new number, `credited_invoice_id`, status Issued, `issue_date = today`) → Show page displays credit-note link.
- **Flow (overdue cron):** `app:mark-overdue-invoices` daily → `Invoice::withoutGlobalScope(TenantScope)` where Issued + due_date < today → flip to Overdue (no notification yet — gap noted).
- **Depends on:** `identity-tenancy` (tenant_id FK, permission gate), `subscription-plans` (route middleware `feature:invoices`), `clients` (Client FK, snapshot source), `objects` (CleaningObject FK, snapshot source), `quotes` (optional quote_id FK backlink, set by Quote::convertToInvoice).
- **Depended on by:** (reserved) Job model will FK invoice_id when implemented. `recurring-invoices` (RecurringInvoice templates generate Invoice rows). `quotes` (Quote::convertToInvoice creates Invoice with quote_id backlink).
- **If you change Core, check:** `InvoiceController::create/edit` (option props: vatRateOptions, paymentTypeOptions, currencyOptions, roundingModeOptions, invoiceDefaults), `InvoiceService` (syncItems per-item VAT/discount calc, computeTotals VAT recap + rounding + balance_due, cancel/duplicate item + field copy/negate, **when quote_id passed store it**), `InvoicePolicy`, `routes/web.php` (10 named routes, `feature:invoices` middleware), `InvoiceNumberService` format placeholders, `RendersInvoicePdf` interface, `InvoicePdfService::render` (VAT recap table in totals partial, per-row discount/vat_rate in items partial, SK fields/QR currency guard), PDF Blade partials (items-table: discount col + vat_rate col + line_base; totals: VAT recap per rate + deposit/balance_due rows; header/dates/footer: payment_type/KS/ŠS/SWIFT/currency/rounding/header_text/footer_text), `PayBySquareService` (balance_due amount, currency guard EUR-only, setSwift BIC), `InvoiceSettingsService` (Tenant swift_bic persist, TenantInterface default_* persist), `InvoiceIssued` notification (`#[Tries]/#[Backoff]/#[Timeout]` attrs + PDF attachment), `StampInvoiceSentAt` listener (AppServiceProvider `Event::listen` registration — changing InvoiceIssued class name breaks the instanceof check), `InvoiceNumberSequence` (per-year sequence still valid), `MarkOverdueInvoices` command (dispatches `InvoiceOverdue` via `NotificationRecipientResolver`), `Invoice` model (quote() BelongsTo relation), test suite (VAT calc + SK fields + deposit + rounding + credit-note negation), enums (PaymentTypeEnum/CurrencyEnum/RoundingModeEnum label + options), i18n keys (all payment_type/currency/rounding_mode/invoices.detail/invoices.vat_recap/invoices.pdf variants), `AppServiceProvider`, Docker rebuild (Chromium layer still needed).
- **Keywords:** faktúra, mesačná/jednorazová/špeciálna, vystavená, uhradená, po splatnosti, stornovaná, dobropis, DPH, IČO, DIČ, IBAN, pay-by-square QR, PDF, zápis v registri, delivery_date, variable_symbol, per-item VAT, discount, VAT recap, deposit, balance_due, rounding (none/document/cash005), payment_type (transfer/cash/card/cod/other), currency (EUR/CZK/USD), constant_symbol, specific_symbol, SWIFT BIC, header_text, footer_text, SK-standard compliance.
- **Gap:** overdue alerts now dispatched via `InvoiceOverdue` notification (notifications domain). Automatic payment matching (Phase 2). Proforma invoice (zálohová faktúra) + credit memo (zálohová faktúra storno) deferred as follow-up feature.

### recurring-invoices

Template-driven auto-generation of invoices on a schedule (nested sub-domain of invoices).

- **Core:** `App\Models\RecurringInvoice` (UUIDv7, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity) + `App\Models\RecurringInvoiceItem` (UUIDv7, tenant_id FK, cascadeOnDelete via recurring_invoice_id, no soft-delete, **added:** `vat_rate`, `discount_percent`). Fields: client_id (nullable), cleaning_object_id (nullable), type (RecurringInvoiceTypeEnum: mirrors InvoiceTypeEnum), frequency (RecurringFrequencyEnum: monthly/every_2_months/quarterly/semi_annually/annually), start_date, end_date (nullable), occurrences_limit (nullable), occurrences_generated (int, incremented by job), next_run_at (Carbon), status (RecurringInvoiceStatusEnum: active|paused|completed|cancelled, `isRunnable()` guard), auto_issue (bool, default false), customer_representative (nullable), notes, **SK fields (added):** `constant_symbol` (nullable), `payment_type` (PaymentTypeEnum, default transfer), `currency` (CurrencyEnum, default EUR), `rounding_mode` (RoundingModeEnum, default none), `header_text` (nullable), `footer_text` (nullable), **deposit (added).** `App\Services\RecurringInvoiceService` (final readonly) — **`paginate(RecurringInvoiceIndexFilterData)`** (eager-loads `['client', 'cleaningObject.client']` — N+1 safe for `customer_display_name`), `create(RecurringInvoiceUpsertData)`, `update(RecurringInvoice, RecurringInvoiceUpsertData)`, `delete(RecurringInvoice)`, `pause(RecurringInvoice)`, `resume(RecurringInvoice)`, `cancel(RecurringInvoice)`, **`generateInvoiceFromTemplate(RecurringInvoice): Invoice`** (passes item vat_rate/discount_percent + all SK fields + deposit into InvoiceUpsertData, computes next_run_at, delegates item copying + invoice creation to existing `InvoiceService::create`, applies recurring_invoice_id link). Lifecycle actions guard terminal states (ValidationException on invalid transitions). `computeInitialNextRunAt()` helper (start_date today/future/past handling).
- **Satellites:**
  - `App\Enums\{RecurringFrequencyEnum, RecurringInvoiceStatusEnum, RecurringDefaultStateEnum}` — `#[TypeScript]`. Frequency: `Monthly|Every_2_Months|Quarterly|SemiAnnually|Annually` with `monthsInterval(): int` (1/2/3/6/12) + `nextRunDate(Carbon $current, int $dayOfMonth): Carbon`. Status: `Active|Paused|Completed|Cancelled`, `isRunnable(): bool` (Active only). DefaultState: `Draft|Issued` (tenant setting).
  - `App\Data\RecurringInvoices\{RecurringInvoiceUpsertData, RecurringInvoiceItemData, RecurringInvoiceListItemData, RecurringInvoiceDetailData, RecurringInvoiceIndexFilterData}` — DTOs per lifecycle. UpsertData: single DTO for store+update, includes 3-way termination radio (forever / until_date / count) with mutual exclusivity validation. **ItemData (updated):** added `float $vat_rate`, `float $discount_percent` (mirrors per-item VAT/discount from invoices; no totals). **UpsertData (updated):** added `float $deposit`, **SK fields:** `?string $constant_symbol`, `PaymentTypeEnum $payment_type`, `CurrencyEnum $currency`, `RoundingModeEnum $rounding_mode`, `?string $header_text`, `?string $footer_text`. IndexFilterData: filters search, status, type, client_id. **ListItemData:** row display (name, frequency, next_run_at, status, occurrences) + **`customer_display_name: ?string`** (resolved: `client?->name ?? cleaningObject?->client?->name ?? customer_name`; non-null for client/object-linked templates; null only for standalone with no manual name — never "Bez odberateľa" bug). **DetailData:** full template + items + generated-invoice count + same `customer_display_name` resolved field (`fromModel` uses `loadMissing(['client', 'cleaningObject.client'])`). Note: `customer_name` is unchanged — still the standalone-mode form field and snapshot pass-through to `InvoiceService::create`.
  - `App\Policies\RecurringInvoicePolicy` — gates viewAny/view → `ViewRecurringInvoices`; create → `CreateRecurringInvoices`; update → `EditRecurringInvoices`; delete → `DeleteRecurringInvoices`.
  - `App\Http\Controllers\RecurringInvoiceController` — thin, `#[Authorize]` per action. 10 routes: index/create/store/show/edit/update/destroy + POST pause/resume/cancel (lifecycle actions, 303 See Other). Feature-gated `feature:invoices` in routes/web.php.
  - `App\Console\Commands\GenerateRecurringInvoices` — daily scheduled (routes/console.php). Queries `RecurringInvoice::withoutGlobalScope(TenantScope)` where status=Active + next_run_at ≤ today. Dispatches one `GenerateRecurringInvoiceJob` per row (queued, afterCommit). Idempotent (re-check in job).
  - `App\Jobs\GenerateRecurringInvoiceJob` (queued, `ShouldBeUnique` with `uniqueId = recurring_invoice_id` + `uniqueFor = 3600` seconds) — `#[Tries(3)]`, `#[Backoff]`, `#[Timeout(120)]`. On handle: binds `app('current_tenant_id')` + `setPermissionsTeamId` (runs outside HTTP), in-handle idempotency re-check (in case job reschedules), calls `RecurringInvoiceService::generateInvoiceFromTemplate()` (creates Invoice, optionally auto-issues per template.auto_issue OR tenant interface.recurring_default_state), increments occurrences_generated, advances next_run_at (via Frequency helper), marks Completed if limit/end_date reached, posts failure context log on exception.
  - `TenantInterface` new column: `recurring_default_state` (RecurringDefaultStateEnum cast, default 'draft'). `InvoiceSettingsService` now updates this field.
  - `Invoice` model new column: `recurring_invoice_id` (UUID FK, nullable, ON DELETE SET NULL). `recurringInvoice() BelongsTo` relation. Captured in `InvoiceDetailData` for "generated from template X" display.
  - FE: `Pages/RecurringInvoices/{Index,Create,Edit,Show}.vue` + components (`RecurringInvoiceForm` with 3-way termination radio + deposit field + SK fields, `RecurringInvoiceItemsEditor` with per-row VAT-rate + discount columns, `RecurringInvoiceFiltersBar`, `RecurringFrequencyBadge`, `RecurringStatusBadge`). Settings page `Pages/Settings/Invoicing.vue` gained `recurring_default_state` toggle (draft vs issued). AppLayout nav: "Opakované faktúry" entry, gated `view recurring_invoices` + `feature:invoices`. `Composables/useRecurringInvoiceFilters.ts` state mgmt for filter input. **Updated:** Create/Edit actions pass option props (vatRateOptions, paymentTypeOptions, currencyOptions, roundingModeOptions) + invoiceDefaults for prefill. Show/Index render `customer_display_name` (resolved DTO field; no client-side fallback needed).
  - i18n keys: `recurring_invoices.*`, `recurring_frequency.*`, `recurring_status.*`, `recurring_default_state.*`.
  - Tests: 6 files covering frequency enum helpers, service lifecycle (pause/resume/cancel with guards), job idempotency + tenant binding, command scheduling, controller authorization, settings integration. ~63 tests total.
- **Flow (create):** `GET /recurring-invoices/create` (middleware `feature:invoices`, `#[Authorize('create', RecurringInvoice)]`) → props: clients, objects, enum options (VAT rates, payment types, currencies, rounding modes), invoiceDefaults (prefilled from TenantInterface) → `Pages/RecurringInvoices/Create.vue` → `RecurringInvoiceForm` (3-way termination radio, deposit field, SK fields with defaults) → POST `/recurring-invoices` → `RecurringInvoiceService::create` transaction: resolve Client/CleaningObject (tenant-scoped), compute initial next_run_at, persist → redirect `/recurring-invoices/{id}` with flash.
- **Flow (auto-generate):** `app:generate-recurring-invoices` (daily, routes/console.php) → finds Active rows with next_run_at ≤ today → `GenerateRecurringInvoiceJob` dispatched (queued, ShouldBeUnique) → on job handle: binds tenant context, calls `generateInvoiceFromTemplate()` (passes recurring item vat_rate/discount_percent + all SK fields + deposit into InvoiceUpsertData) → `InvoiceService::create` (reuses existing logic with VAT/SK support, no duplication), optionally auto-issues, increments counter, advances next_run_at (Frequency::nextRunDate helper), marks Completed on termination, catches + logs exception.
- **Depends on:** `identity-tenancy` (tenant_id FK, permission gate), `subscription-plans` (route middleware `feature:invoices`), `invoices` (reuses InvoiceService::create, shares FrequencyEnum, shares PDF + Pay-by-Square logic).
- **Depended on by:** `invoices` (Invoice.recurring_invoice_id FK, display "generated from template X").
- **If you change Core, check:** `RecurringInvoiceController::create/edit` (option props: vatRateOptions, paymentTypeOptions, currencyOptions, roundingModeOptions, invoiceDefaults), `RecurringInvoicePolicy`, `routes/web.php` (10 named routes, `feature:invoices` middleware), `RecurringFrequencyEnum` helper methods (month intervals, next_run_at calculation), `RecurringInvoiceService` lifecycle guards (isRunnable check on transitions), **`generateInvoiceFromTemplate`** (maps vat_rate/discount_percent from items, passes SK fields + deposit to InvoiceUpsertData), `GenerateRecurringInvoiceJob` tenant context binding + idempotency + auto-issue logic + item field mapping, `GenerateRecurringInvoices` command (scheduling in routes/console.php, scope-bypass), `TenantInterface` (recurring_default_state fillable + cast), `InvoiceService::create` (recurring_invoice_id link on new Invoice, VAT + SK field support), Invoice model (recurring_invoice_id relation), `InvoiceSettingsService` (TenantInterface.recurring_default_state + default_* + swift_bic persist), `AppServiceProvider` (RecurringInvoicePolicy binding if explicit), test job + command + controller + service (VAT/SK fields/deposit propagation, idempotency coverage), `RecurringInvoiceService::paginate` eager-load list + `RecurringInvoiceDetailData::fromModel loadMissing` list (both must include `['client', 'cleaningObject.client']` — drives `customer_display_name`; update both if relation names change), `RecurringInvoices/{Show,Index}.vue` (`customer_display_name` prop binding).
- **Keywords:** opakované faktúry, šablóna, frekvencia, mesačne/štvrťročne, ročne, automatic generation, schedule, next_run_at, termination modes (forever / until / count), auto_issue, job queue, ShouldBeUnique, daily command, generated_invoice_id link, per-item VAT/discount inheritance, SK fields inheritance, deposit inheritance, payment_type/currency/rounding propagation.

### contracts

Template-driven contract management with polymorphic subjects and token-based body filling.

- **Core:** `App\Models\ContractTemplate` (UUID, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity, `name`, `body` longtext with `{{placeholders}}`, `category` ContractCategoryEnum, `is_active` bool) + `App\Models\Contract` (UUID, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity, polymorphic `contractable` MorphTo → CleaningObject|TenantMembership, `contract_template_id` FK nullable, `category` ContractCategoryEnum, `status` ContractStatusEnum, `term_type` ContractTermTypeEnum, `title`, `reference_number`, `body` snapshot, `valid_from`, `end_date` nullable, `signed_at`, `terminated_at`, `termination_reason`, `notes`; `isEditable()/canBeSigned()/canBeTerminated()` guards; body immutable after sign) + `App\Models\EmploymentContract` (child of Contract via `contract_id` FK, cascadeOnDelete; `type` EmploymentContractTypeEnum DPP|DPČ|TPP|Živnosť, `position`, `salary`, `hours`, `probation_end_date`). `App\Services\ContractTemplateService` (final readonly) — paginate/create/update/delete. `App\Services\ContractService` (final readonly) — paginate/create/update/sign/terminate/delete, cross-field guards (end_date required for fixed term, EmploymentContract required for employment category on BOTH create+update; orphan EmploymentContract deleted on category change). `App\Services\PlaceholderResolverService` (final readonly) — tenant-safe `resolve(body, contractable): string` token substitution + `catalogFor(kind): array` per contract kind.
- **Satellites:**
  - `App\Enums\{ContractStatusEnum, ContractTermTypeEnum, ContractCategoryEnum, EmploymentContractTypeEnum}` — `#[TypeScript]`. Status: Draft|Active|Expired|Terminated, `canTransitionTo()` + `isEditable()/canBeSigned()/canBeTerminated()` methods. TermType: Fixed|Indefinite. Category: ServiceAgreement|Employment|NDA|GDPR|Other. EmploymentType: DPP|DPČ|TPP|SelfEmployed.
  - `App\Data\Contracts\{ContractUpsertData, ContractIssueData [renamed to SignData], ContractTerminateData, ContractListItemData, ContractDetailData, ContractIndexFilterData, EmploymentContractData, EmploymentContractUpsertData}` + `App\Data\ContractTemplates\{ContractTemplateUpsertData, ContractTemplateListItemData, ContractTemplateDetailData, ContractTemplateIndexFilterData}`. UpsertData validates `contractable_id`/`contractable_type` + `contract_template_id` scoped to current tenant, **added** `?string $quote_id` (optional backlink to source Quote). ContractDetailData: `contractable_id`, `contractable_type` fields (distinguishes CleaningObject vs TenantMembership for FE polymorphic rendering).
  - `App\Contracts\RendersContractPdf` interface (DIP) — `render(Contract, TemplateEnum?): Stream` seam for PDF generation. Bound in `AppServiceProvider`.
  - `App\Services\Pdf\ContractPdfService` — implements `RendersContractPdf`. Uses `spatie/laravel-pdf` with `chrome` driver; mirrors invoice PDF infra (Blade-based, arm64 Chromium via Playwright). Blade views in `resources/views/pdf/contracts/{template}.blade.php`. Eagerly loads contractable (via `eagerLoadForRender()` method) to resolve names for token substitution in the PDF.
  - `App\Policies\ContractPolicy` (viewAny/view/create/update/delete/sign/terminate/downloadPdf) + `App\Policies\ContractTemplatePolicy` (viewAny/view/create/update/delete). rbac-full: `#[Authorize]` per controller action. New PermissionEnum cases: ViewContracts/CreateContracts/EditContracts/TerminateContracts/DeleteContracts + ViewContractTemplates/CreateContractTemplates/EditContractTemplates/DeleteContractTemplates. Seeded in `RoleTemplatesSeeder`: Vlastník all; Sekretárka contracts (view/create/edit/terminate), contract_templates (all); Účtovníčka contracts (view).
  - `App\Http\Controllers\{ContractController, ContractTemplateController}` — thin, `#[Authorize]` per action. Contracts: index/create/store/show/edit/update/destroy/sign/terminate/pdf (10 routes). Templates: resource (5 routes). 15 named routes behind `auth` + `feature:contracts`.
  - `App\Console\Commands\CheckContractExpiry` — daily scheduled (routes/console.php). Fixed-term Active contracts past `end_date` → Expired + dispatches `ContractExpired` notification to `view contracts` holders via `NotificationRecipientResolver` + structured log. Upcoming 30/14/7d → `ContractExpiring` notification (with `$days` param) + structured log.
  - Tenant-safety: `ContractUpsertData` validates `contractable_id`/`contractable_type` + `contract_template_id` scoped to current tenant; membership picker in controller filters `->where('tenant_id', app('current_tenant_id'))` (TenantMembership is excluded from BelongsToTenant trait, so manual filter applied).
  - FE: `Pages/Contracts/{Index,Create,Edit,Show}`, `Pages/ContractTemplates/{Index,Create,Edit,Show}` (mirror invoices). Components: `Contracts/{ContractForm, ContractStatusBadge, PlaceholderTokenList, ContractBodyEditor [textarea + insert-at-cursor], ContractFiltersBar, EmploymentContractFields}`, `ContractTemplates/ContractTemplateForm`. Composables: `useContractFilters`, `useContractTemplateFilters`. AppLayout nav: "Zmluvy" + "Šablóny zmlúv" entries, gated `view contracts`/`view contract_templates` + `feature:contracts`. Button visibility driven by BE flags `is_editable`/`can_be_signed`/`can_be_terminated`. i18n keys added in `lang/{sk,en,uk}/app.php`.
  - Routes feature-gated `feature:contracts` (Pro/Enterprise): `contracts.{index,create,store,show,edit,update,destroy,sign,terminate,pdf}` + `contract-templates.{index,create,store,show,edit,update,destroy}`.
- **Flow (create from template):** `GET /contracts/create` (middleware `feature:contracts`, `#[Authorize('create', Contract)]`) → props: clients list (for object picker), contract templates list (with bodies), membership options (for employment-contract picker), enum options → `Pages/Contracts/Create.vue` → `ContractForm` (category-aware field conditionals: object picker for service_agreement, membership + employment fields for employment, etc.) → choose template → body pre-fills with tokens → POST `/contracts` (ContractUpsertData) → `ContractService::create` transaction: resolve CleaningObject/TenantMembership (tenant-scoped), snapshot body (copy + token-substitute), create Contract + optionally EmploymentContract if category=employment + employment fields present → redirect `/contracts/{id}`.
- **Flow (sign):** `POST /contracts/{id}/sign` → `ContractService::sign` (guard `canBeSigned()`) → status Draft→Active, set `signed_at` → render PDF in view prep → Show page displays PDF download link.
- **Flow (PDF):** `GET /contracts/{id}/pdf` → `#[Authorize('view', …)]` → `ContractPdfService::render` (body + token catalog for reference) → `spatie/laravel-pdf` chrome driver → streamDownload (filename: title or 'contract').
- **Flow (terminate):** `POST /contracts/{id}/terminate` (guard `canBeTerminated()`) → `ContractService::terminate` → status Active|Expired→Terminated, set `terminated_at` + optional `termination_reason` → redirect with flash.
- **Flow (expiry cron):** `app:check-contract-expiry` daily → `Contract::withoutGlobalScope(TenantScope)` where status=Active + term_type=Fixed + end_date < today → flip to Expired → dispatch `ContractExpired` notification to `view contracts` holders + structured log. Upcoming exactly 30/14/7d → `ContractExpiring` notification + structured log.
- **Depends on:** `identity-tenancy` (tenant_id FK, permission gate), `subscription-plans` (route middleware `feature:contracts`), `objects` (CleaningObject polymorphic subject), implicit on TenantMembership (polymorphic subject, excluded from BelongsToTenant but used in morph map), `quotes` (optional quote_id FK backlink, set by Quote::convertToContract).
- **Depended on by:** (reserved) future Job/Schedule may link to Contract. `quotes` (Quote::convertToContract creates Contract with quote_id backlink).
- **If you change Core, check:** `ContractController::create/edit` (option props: templates, enum options, membership list filtered by tenant), `ContractPolicy` methods, `routes/web.php` (15 named routes, `feature:contracts` middleware), `ContractService` (sign/terminate/delete state transitions, category-change orphan EmploymentContract logic, cross-field guards, **when quote_id passed store it**), `PlaceholderResolverService::catalogFor` token list per kind, `ContractPdfService::render` (token catalog reference in PDF, contractable eager-load), `CheckContractExpiry` command (scope-bypass + Fixed-term date check logic), `EmploymentContractTypeEnum` (DPP→dpp etc. name mappings for i18n), `Contract` model (quote() BelongsTo relation), test suite (polymorphic subject resolution, template token substitution, state machine guards, PDF rendering), enums (ContractStatusEnum/TermTypeEnum/CategoryEnum/EmploymentTypeEnum label + options + canTransition logic), i18n keys (all contract_*/*.*, employment_contract_type.*, status.*, term_type.*), `RoleTemplatesSeeder` (Sekretárka/Účtovníčka contracts/templates perms, no change needed if seeded as spec-planned), `AppServiceProvider` (explicit policy bindings).
- **Keywords:** zmluva, šablóna, klient, zamestnanec, DPP/DPČ/TPP, živnosť, {{placeholder}}, token, body snapshot, polymorphic, CleaningObject, TenantMembership, term_type (fixed/indefinite), status (draft/active/expired/terminated), sign, terminate, expire, PDF, daily check, Pro/Enterprise.

### capabilities

Client-side authorization + entitlement layer. Queries server for runtime permission + feature check (two-axis AND model: user/permission axis AND plan/feature axis) + remaining tenant-creation quota.

- **Core:** `resources/js/stores/capabilities.ts` (Pinia Options store) — state: permissions, features, accountPlan, remainingTenantSlots, loaded (bool); actions: fetch, ensureLoaded, reset. Hydrated async from `GET /api/me`.
- **Satellites:**
  - `resources/js/services/MeService.ts` — `GET /api/me` over window.axios (session-auth XHR, returns `MeData` from BE DTO with accountPlan + remainingTenantSlots).
  - `resources/js/composables/useAuthorization.ts` — read-only composable `can(permission)`, `hasFeature(feature)`, `allows(permission, feature)` (AND semantics), `canCreateTenant(): bool` (checks remainingTenantSlots > 0). Reads from store.
  - `resources/js/Components/Can.vue` — declarative slot-based guard: `<Can permission feature>` (both optional, both AND if present) + `#fallback`. Render conditionally or hide element.
  - `resources/js/lib/routeRequirements.ts` — path-prefix → {permission?, feature?} map for route-level early guard.
  - `AppLayout.vue` tenant dropdown — conditionally disables "Pridať novú firmu" affordance if `!useAuthorization().canCreateTenant()`.
- **Flow (page load):** App.vue mounted → `useAuthorization().ensureLoaded()` (sync cache check, fail-open on first nav) → Inertia `router.on('before')` reads store, denies redirect to /dashboard if lacking capability. `navigate` hook async-awaits `ensureLoaded()`. `AppLayout.vue` logout calls store `reset()`.
- **Flow (add tenant UX):** AppLayout tenant dropdown displays active tenant + available tenants + "Pridať novú firmu" link. Link disabled (grayed) if `remainingTenantSlots <= 0`. Click → AddTenantModal dialog. Submit → `TenantController@store` server-side canCreateTenant check (403 if over limit, FE shows `app.tenant.limit_reached`). On success, tenant added to list, store re-synced on next navigation.
- **Depends on:** `identity-tenancy` (MeController, /api/me endpoint); `subscription-plans` (features list, max_tenants quota).
- **Depended on by:** every page that needs UI capability gating (client-side render condition via `Can` or composable `can()`); AppLayout (canCreateTenant disabling).
- **If you change Core, check:** `MeController` (accountPlan + remainingTenantSlots hydration = maxTenants - ownedTenants.count), `ChecksFeatures::maxTenants` (plan config lookup), `routes/api.php` (/api/me route), FE store (state shape), FE composable `canCreateTenant`, AppLayout tenant dropdown UX disable logic.
- **Keywords:** permission, feature, can, allows, capability, entitlement, two-axis AND, render-by-capability, Can component, useAuthorization.

### employees

Employee management layer over `TenantMembership` (decision: employee = User + TenantMembership pivot, NO new core model). CRUD: list (filters search/role/is_active, sort -joined_at), create (new or link User by email), edit (profile + role + permission overrides + employment contract), show, deactivate (is_active=false, not delete).

- **Core:** `App\Models\TenantMembership` (existing pivot, extended with profile columns) + `App\Services\EmployeeService` (final readonly) — `paginate(EmployeeIndexFilterData)`, `create(EmployeeUpsertData)`, `update(TenantMembership, EmployeeUpsertData)`, `deactivate(TenantMembership)`. **Security guards:** permission-grant intersect with actor's own permissions (prevent escalation), role-assignment subset guard (assigned role's permissions ⊆ actor's permissions — prevents privilege escalation). Quota gate (MultiUser feature) on create.
- **Satellites:**
  - `App\Models\TenantMembership` — extended: nullable profile columns `first_name`, `last_name`, `phone`, `position`; `LogsActivity`, `employmentContract() MorphMany Contract where category=Employment`, `displayName` accessor (first_name + last_name or user.name fallback). Still deliberately EXCLUDED from `BelongsToTenant` trait — scoped manually in service (tenant_id in WHERE clause) to keep TenantMembership pivot-like. User relation unchanged. `joined_at` timestamp.
  - `App\Data\Employees\{EmployeeIndexFilterData, EmployeeListItemData, EmployeeDetailData, EmployeeUpsertData}` — DTOs per CRUD. UpsertData: `email`, `?string first_name`, `?string last_name`, `?string phone`, `string role_name`, `array permissions` (direct permission overrides, intersected with actor's own), `?EmploymentContractUpsertData employment` (optional draft contract creation). EmploymentContractUpsertData: `EmploymentContractTypeEnum employment_type`, `?string position`, `?float hourly_rate`, `?float monthly_salary`, `?int weekly_hours`, `?Carbon probation_end_date`. ListItemData + DetailData: resolved role names (Spatie Role model), permissions list, `displayName` (accessor).
  - `App\Policies\TenantMembershipPolicy` (rbac-full) — `viewAny/view/create/update/delete` gates on `view|create|edit|delete employees` permissions, with tenant_id check on record-level. Registered explicitly in `AppServiceProvider::boot()` via `Gate::policy(TenantMembership::class, TenantMembershipPolicy::class)`.
  - `App\Http\Controllers\EmployeeController` (thin, `#[Authorize]` per action) — 7 routes: index/create/store/show/edit/update + POST deactivate. Create/Edit return `roleOptions` (all roles per tenant), `permissionGroups` (grouped PermissionEnum), `employmentTypeOptions` (EmploymentContractTypeEnum). Store action calls `EmployeeService::create` (transaction: resolve/create User, guard escalation, assign role, optionally create draft Contract, send InvitationCreated mail to new users). Update action calls `EmployeeService::update` (transaction: update profile, re-assign role, resync permission overrides, NOT employment contract).
  - Routes feature-gated `feature:employees` (Pro/Enterprise) + auth. All routes `whereUuid('employee')` on TenantMembership.
  - `App\Enums\PermissionEnum` — added 5 cases: `ViewEmployees`, `CreateEmployees`, `EditEmployees`, `AssignEmployees` (semantically: role assignment authority), `DeleteEmployees`. `label()` method returns i18n key `app.permission.<value>` (new common method across PermissionEnum).
  - `RoleTemplatesSeeder` — Vedúca (Supervisor) gains `ViewEmployees` + `AssignEmployees`; Vlastník all. Sekretárka/Účtovníčka no change (no employees perms per spec). Seeded per tenant in `seedForTenant()` static method.
  - FE: `Pages/Employees/{Index,Create,Edit,Show}.vue` (separate pages, not drawers, mirroring Contracts structure). Components: `EmployeeForm` (profile fields + role picker + permission checkboxes grouped + optional EmploymentContractFields, mirrors ContractForm pattern), `EmployeeFiltersBar`, `PermissionCheckboxGroups` (reused from ContractForm design), `EmployeeStatusBadge` (Active/Deactivated), `RoleOptionRow` (with permission indicator). Composable: `useEmployeeFilters` (state mgmt). AppLayout nav: "Zamestnanci" entry, gated `view employees` + `feature:employees`.
  - i18n keys: `employees.*`, `employee_role.*`, `permission.*` (all 5 new + existing ones mapped to labels), `permission_group.*` (frontend grouping headers: clients/objects/quotes/invoices/contracts/employees/schedule/etc.).
  - Tests: `tests/Feature/Employees/{EmployeeServiceTest, EmployeePolicyTest, EmployeeControllerTest}` — 30+ tests covering: service lifecycle (create/update/deactivate), role escalation guard (forbid assigning role with permissions actor lacks), permission-grant intersect (only grant perms actor has), quota enforcement (MultiUser feature limit), existing-user reactivation, new-user creation + InvitationCreated mail, policy authorization, controller CRUD authorization.
- **Flow (list):** `GET /employees` (middleware `feature:employees`, `#[Authorize('viewAny', TenantMembership)]`) → `EmployeeService::paginate` (QueryBuilder filters search/role/is_active, sorts -joined_at, eager-loads user + roles) → `EmployeeListItemData::collect(...)` → Inertia `Employees/Index`.
- **Flow (create):** `GET /employees/create` (middleware `feature:employees`, `#[Authorize('create', TenantMembership)]`) → props: all roles, all permissions grouped, employment type options → `Pages/Employees/Create.vue` → `EmployeeForm` → POST `/employees` (EmployeeUpsertData) → `EmployeeService::create` transaction: quota guard (MultiUser), role escalation guard, permission intersect guard, resolve-or-create User (new users auto-verified, Free plan, get InvitationCreated mail + random password), create TenantMembership (or reactivate inactive), assign role within tenant scope, sync direct permission overrides (intersected with actor), optionally create draft Contract linked to membership → redirect with flash.
- **Flow (update):** `GET|POST /employees/{id}/edit|update` → similar guards on role/permission assignment; update profile fields + role + permissions (wipe if empty, never touches employment contract) → refresh and redirect.
- **Flow (deactivate):** `POST /employees/{id}/deactivate` → `EmployeeService::deactivate` (is_active=false) → user loses access to tenant, not deleted.
- **Depends on:** `identity-tenancy` (tenant_id FK, TenantMembership pivot, permission gate), `subscription-plans` (route middleware `feature:employees`, quota guard on MultiUser feature), `invitations` (InvitationCreated mail for new users).
- **Depended on by:** `contracts` (Contract polymorphic to TenantMembership for employment contracts, linked via membership.id).
- **If you change Core, check:** `EmployeeController` (7 actions, role/permission option props), `EmployeeService` (escalation guards on create + update, permission intersect logic, quota check, new-user creation with Free plan + email_verified_at), `TenantMembershipPolicy` (all gates + tenant_id checks), `routes/web.php` (7 named routes, `feature:employees` middleware), `App\Scopes\TenantScope` (must NOT apply to TenantMembership — manual scoping in EmployeeService), `RoleTemplatesSeeder::seedForTenant` (Vedúca + Vlastník employees perms), `PermissionEnum` (label() method must be available on all cases), `AppServiceProvider` (`Gate::policy(TenantMembership, TenantMembershipPolicy)` explicit binding), `InvitationCreated` notification (dispatched from EmployeeService::create for new users), test suite (escalation guard + permission intersect coverage).
- **Keywords:** zamestnanec, employee, profile (first_name, last_name, phone, position), role assignment, permission override, quota (MultiUser), escalation guard, deactivate, membership reactivation, employment contract (draft), feature:employees, Pro/Enterprise.

### notifications

Native Laravel notification system serving two audiences: internal tenant users (stored as `DatabaseNotification` rows + optional mail per user prefs) and external recipients (mail-only, on-demand, no DB row, no prefs).

- **Core:** `App\Enums\NotificationTypeEnum` (`#[TypeScript]`, 8 cases: `invitation.created`, `invoice.issued`, `invoice.overdue`, `contract.expiring`, `contract.expired`, `quote.sent`, `quote.expiring`, `quote.expired`). Two groups: **external mail-only** (`InvitationCreated`, `InvoiceIssued` — `deliversInApp()=false`, not user-configurable) and **internal in-app** (`InvoiceOverdue`, `ContractExpiring`, `ContractExpired`, `QuoteSent`, `QuoteExpiring`, `QuoteExpired`). `App\Notifications\BaseTenantNotification` abstract (extends `Notification`, implements `ShouldQueue`, uses `Queueable`, `afterCommit()`) — `via()` returns `[TenantDatabaseChannel::class]` + optional `'mail'`; `mailEnabledFor()` reads `$notifiable->notification_preferences[$type]['mail']` falling back to `defaultMailEnabled()` (only `InvoiceOverdue=true` by default). 6 internal concrete subclasses (`InvoiceOverdue`, `ContractExpiring`, `ContractExpired`, `QuoteSent`, `QuoteExpiring`, `QuoteExpired`). 2 external: `InvoiceIssued` (`#[Tries(3)]`, `#[Backoff([10,30,60])]`, `#[Timeout(120)]`, mail-only, PDF attached via `RendersInvoicePdf`, `failed()` logs), `InvitationCreated` (mail-only, dispatched via `Notification::route('mail', $email)`). `App\Notifications\Channels\TenantDatabaseChannel` extends `DatabaseChannel` — overrides `buildPayload()` to inject `tenant_id` from `$notification->tenantId()`. `App\Services\NotificationService` (final readonly) — `paginate(filter, user, tenantId)`, `bell(user, tenantId): NotificationBellData` (unreadCount + 5 recent), `markRead`, `markAllRead`, `updatePreferences`. **Manual tenant scoping**: all queries use `$user->notifications()->where('tenant_id', $tenantId)` (no BelongsToTenant). `App\Services\NotificationRecipientResolver` (final readonly) — `usersWithPermission(tenantId, PermissionEnum): Collection<User>`: calls `setPermissionsTeamId($tenantId)` then filters active tenant members by `$user->can($permission->value)`.
- **Satellites:**
  - `notifications` table — framework `DatabaseNotification` schema + `tenant_id` UUID FK (CASCADE on tenant delete) + composite index `(notifiable_type, notifiable_id, tenant_id, read_at)`. **NOT** `BelongsToTenant` / `HasUuids` — framework-owned; tenant isolation enforced manually (see Gotchas). Migration: `2026_06_21_224710_create_notifications_table.php`.
  - `users.notification_preferences` — JSON column (nullable). Shape: `{type: {mail: bool}}`. Added in users migration.
  - `App\Data\Notifications\{NotificationBellData, NotificationListItemData, NotificationIndexFilterData, NotificationPreferencesData, NotificationPreferencesUpdateData, NotificationPreferenceItemData}` — DTOs (`#[TypeScript]`). `NotificationBellData`: `unreadCount: int`, `recent: NotificationListItemData[]`.
  - `App\Policies\NotificationPolicy` — `viewAny`: `view notifications`; `update` (mark-read): `view notifications` + `notifiable_id === $user->id` + `tenant_id === app('current_tenant_id')`. Registered explicitly in `AppServiceProvider` via `Gate::policy(DatabaseNotification::class, NotificationPolicy::class)` (no auto-discovery for framework model).
  - `App\Http\Controllers\NotificationController` — `index` (`#[Authorize('viewAny', DatabaseNotification)]`), `markRead` (`#[Authorize('update', 'notification')]`), `markAllRead`, `settings`, `updateSettings`. 5 web routes.
  - `App\Http\Controllers\Api\NotificationBellController` — single `__invoke`, `GET /api/notifications/bell` → `NotificationBellData`. No `#[Authorize]` (mirrors `MeController`: auth + TenantContextMiddleware are the gates; self-scoped endpoint).
  - `App\Listeners\StampInvoiceSentAt` — handles `NotificationSent` (framework event), stamps `Invoice.sent_at` when notification is `InvoiceIssued`. Registered in `AppServiceProvider::boot()` via `Event::listen(NotificationSent::class, StampInvoiceSentAt::class)`.
  - `resources/js/stores/notifications.ts` — Pinia Options store: state `unreadCount`, `recent[]`, `loading`, `error`, `pollHandle`; actions: `fetchBell()` (calls `/api/notifications/bell`), `startPolling(60000)` (idempotent), `stopPolling()`, `markReadLocally(id)` (optimistic, decrements count + stamps readAt), `reset()` (stops poll + resets). Polling started in Inertia navigate hook in `app.ts`.
  - `resources/js/services/NotificationBellService.ts` — `fetchBell()` XHR wrapper over `GET /api/notifications/bell`.
  - FE components: `Components/Notifications/{NotificationBell, NotificationItem, NotificationTypeBadge, NotificationPreferenceRow}.vue`. Bell in `AppLayout.vue` header. Pages: `Pages/Notifications/Index.vue`, `Pages/Settings/Notifications.vue`.
  - Permissions: `view notifications` (`PermissionEnum::ViewNotifications`) — Vlastník + Vedúca + Sekretárka + Účtovníčka (seeded in `RoleTemplatesSeeder`). `configure notifications` (`PermissionEnum::ConfigureNotifications`) — defined in `PermissionEnum`, not explicitly seeded per role (settings accessible via auth only). `viewNotifications` added to `HandleInertiaRequests::canPayload` map → shared as `can.viewNotifications` Inertia prop.
  - i18n: `notifications.*`, `notification_type.*`, `notification_settings.*` in `lang/{sk,en,uk}/app.php`.
  - Tests: 6 files in `tests/Feature/Notifications/` — `InvitationEmailTest`, `InvoiceIssuedNotificationTest`, `NotificationBellApiTest`, `NotificationCenterTest`, `NotificationPreferencesTest`, `SystemNotificationDispatchTest`.
- **Flow (internal dispatch, e.g. overdue):** `app:mark-overdue-invoices` daily → flips Invoice to Overdue → groups by tenant → `NotificationRecipientResolver::usersWithPermission(tenantId, ViewInvoices)` → `Notification::send($recipients, new InvoiceOverdue($tenantId, $invoiceId))` (queued, afterCommit) → `TenantDatabaseChannel::buildPayload()` injects `tenant_id` into DB row → optional mail per user prefs.
- **Flow (bell poll):** Inertia navigate hook → `useNotificationsStore.fetchBell()` + `startPolling(60s)` → `GET /api/notifications/bell` (session-auth XHR, auth + TenantContextMiddleware only) → `NotificationBellData` → bell icon shows `unreadCount`, dropdown shows recent 5.
- **Flow (notification centre):** `GET /notifications` (`#[Authorize('viewAny', DatabaseNotification)]`) → `NotificationService::paginate(filter, user, tenantId)` scoped to current tenant → `NotificationListItemData` paginated → `Notifications/Index.vue`.
- **Flow (invoice email):** `POST /invoices/{id}/send` → `InvoiceController` routes `Notification::route('mail', $customer_email)->notify(new InvoiceIssued($invoiceId))` (mail-only, queued, afterCommit) → job loads Invoice, calls `RendersInvoicePdf::render()`, attaches PDF to `MailMessage` → on delivery: `NotificationSent` event → `StampInvoiceSentAt::handle()` stamps `Invoice.sent_at`.
- **Flow (invitation email):** `RegistrationService::createInvitations` → `Notification::route('mail', $invite->email)->notify(new InvitationCreated($token, $tenantName, $roleName))` — mail delivered inline (no DB row, no prefs check).
- **Depends on:** `identity-tenancy` (User `notifications()` HasMany relation, `TenantContextMiddleware` binds tenant for gate), `invoices` (`InvoiceOverdue`, `InvoiceIssued`), `contracts` (`ContractExpiring`, `ContractExpired`), `quotes` (`QuoteSent`, `QuoteExpiring`, `QuoteExpired`), `invitation-accept` (`InvitationCreated`).
- **Depended on by:** `invoices` (`StampInvoiceSentAt` updates `sent_at`); `identity-tenancy` (`can.viewNotifications` Inertia shared prop).
- **If you change Core, check:** `NotificationTypeEnum` (add/remove type → update `deliversInApp()`, `defaultMailEnabled()`, `userConfigurable()`), `TenantDatabaseChannel` (`tenant_id` injection must match `notifications.tenant_id` column type), `NotificationService` (every query MUST filter by `tenant_id` — never bare `$user->notifications()`), `NotificationRecipientResolver` (`setPermissionsTeamId` before `can()` — silent mismatch otherwise), `NotificationPolicy` (`update` checks both `notifiable_id` + `tenant_id`), `AppServiceProvider` (`Gate::policy(DatabaseNotification)` + `Event::listen(NotificationSent, StampInvoiceSentAt)`), `routes/web.php` (5 notification routes, no feature gate — available on all plans), `routes/api.php` (bell endpoint in api middleware group), `HandleInertiaRequests::canPayload` (`viewNotifications` key), cron commands (`MarkOverdueInvoices`/`CheckContractExpiry`/`ExpireQuotes` each inject `NotificationRecipientResolver` + call `Notification::send`), `QuoteService::send` (`QuoteSent` dispatch via resolver), `RegistrationService::createInvitations` (`InvitationCreated` mail dispatch).
- **Keywords:** oznamenie, notifikácia, bell, unread, notification_preferences, TenantDatabaseChannel, in-app, mail-only, InvoiceIssued, InvoiceOverdue, ContractExpiring, ContractExpired, QuoteSent, QuoteExpiring, QuoteExpired, InvitationCreated, StampInvoiceSentAt, NotificationSent event, 60s poll.

### schedule

Work decomposition + materialized job scheduling from signed service-agreement contracts.

- **Core:** `App\Models\WorkBreakdown` (UUID, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity; table `work_breakdowns`; columns: cleaning_object_id FK, contract_id FK, source_quote_id nullable, name, is_active) + `App\Models\WorkBreakdownTask` (UUID, tenant_id FK, BelongsToTenant, no LogsActivity; table `work_breakdown_tasks`; columns: work_breakdown_id FK cascade, name, description, frequency TaskFrequencyEnum, position) + `App\Models\ScheduledJob` (UUID, tenant_id FK, BelongsToTenant, SoftDeletes, LogsActivity; table `cleaning_jobs` — avoids Job/jobs collision; columns: cleaning_object_id FK, assigned_membership_id nullable FK (single), work_breakdown_id nullable FK, work_breakdown_task_id nullable FK, contract_id nullable FK, invoice_id nullable FK (reserved Phase 2), type JobTypeEnum, status JobStatusEnum, scheduled_date, start_time nullable, end_time nullable, note, completed_at nullable, cancelled_at nullable). + `App\Services\WorkBreakdownService` (final readonly) — `generateFromContract(Contract): ?WorkBreakdown` (auto-called by ContractService::sign, idempotent, creates breakdown + child task per quote item when contract.category=ServiceAgreement + contractable is CleaningObject + quote_id present), `paginate()`, `create/update/delete` (CRUD for manual breakdown mgmt, no public UI this phase — read-only on Object.show). + `App\Services\JobService` (final readonly) — `paginate(JobIndexFilterData)` (QueryBuilder eager: cleaningObject.client + assignedMembership.user, filters: status/type/object_id/assigned_membership_id/date_from/date_to, sorts: scheduled_date/status/created_at), `create(JobUpsertData)` (sets status Planned if assigned, Unassigned otherwise), `update(ScheduledJob, JobUpsertData)`, `assign(job, ?membershipId)` (assigns or unassigns, checks canBeAssigned), `cancel(ScheduledJob)` (guard canBeCancelled, stamps cancelled_at), `complete/unapprove(ScheduledJob)` (Phase 2 stubs), **`unassignFutureForMembership(TenantMembership)`** (called by EmployeeService::deactivate, unassigns all future jobs for the member), `generateForBreakdown(WorkBreakdown, CarbonPeriod)` (materialization from recurrence, partial-unique idempotency). `App\Console\Commands\GenerateScheduledJobs` — daily scheduled (routes/console.php) finds active breakdowns with active contracts, dispatches `GenerateScheduledJobsJob` per breakdown. `App\Jobs\GenerateScheduledJobsJob` (queued, ShouldBeUnique, Tries(3), afterCommit) — binds tenant context + calls `setPermissionsTeamId` on worker, materializes scheduled jobs from breakdown tasks per frequency + rolling horizon (config/scheduling.php horizon_days, default 30d), idempotent via partial-unique checking on (work_breakdown_id, work_breakdown_task_id, scheduled_date).
- **Satellites:**
  - `App\Enums\{JobTypeEnum, JobStatusEnum, TaskFrequencyEnum}` — all `#[TypeScript]`. JobTypeEnum: Regular | OneOff | Special + `label()`, `options()`. JobStatusEnum: Planned | Unassigned | InProgress | Completed | Unapproved | Cancelled + `label()`, `options()`, `allowedTransitions()`, `canTransitionTo(to)`, `isEditable()` (not Completed/Cancelled), `isTerminal()` (Completed/Unapproved/Cancelled). TaskFrequencyEnum: OneTime | Weekly1x | Seasonal + `label()`, `options()`, `monthsInterval(): int` (for quarter/semi-annual/annual math), `nextRunDate(from: Carbon): Carbon` (recurrence helper).
  - `App\Data\Schedule\{JobIndexFilterData, JobCalendarFilterData, JobListItemData, JobDetailData, JobUpsertData, JobAssignData, WorkBreakdownDetailData, WorkBreakdownTaskData, WorkBreakdownUpsertData}` — DTOs (`#[TypeScript]`). JobDetailData includes `can` Record<string, bool> (update/assign/cancel computed from policy + model state). WorkBreakdownDetailData has `tasks` array + `contract` nested + read-only.
  - `App\Policies\ScheduledJobPolicy` — rbac-full: viewAny/view→ViewSchedule, create→CreateSchedule, update→EditSchedule + isEditable guard, assign→AssignCleaners + canBeAssigned guard, cancel→EditSchedule + canBeCancelled guard, delete→EditSchedule + isEditable guard.
  - `App\Enums\PermissionEnum` — added: ViewSchedule, CreateSchedule, EditSchedule, AssignCleaners.
  - `App\Http\Controllers\ScheduledJobController` — thin, `#[Authorize]` per action (newable: create/store, readable: show, writable: edit/update/assign/cancel). 8 routes: index/create/store/show/edit/update/assign/cancel. Create/Edit return `objectOptions` (ObjectOptionData[]), `membershipOptions` (TenantMembershipListItemData[] — active members only, scoped to object tenants if selected). Show returns `membershipOptions` + `workBreakdown` (WorkBreakdownDetailData nullable, read-only).
  - `App\Models\CleaningObject` — gained `workBreakdowns() HasMany` relation. ObjectController::show now loads and passes `workBreakdowns` as WorkBreakdownDetailData[].
  - `App\Services\EmployeeService::deactivate()` — calls `$jobService->unassignFutureForMembership($membership)` (Phase 1 hook to prevent future job assignments when employee is deactivated).
  - Routes feature-gated `feature:schedule` (Pro/Enterprise): jobs.{index,create,store,show,edit,update,assign,cancel}.
  - config/scheduling.php — new file. `['horizon_days' => 30]` (rolling window for daily job generation command).
  - FE: `Pages/Schedule/{Index,Create,Edit,Show}.vue` + components `Schedule/{JobStatusBadge, JobTypeBadge, JobFiltersBar, JobList, JobCalendar (fullcalendar @vue/calendar v6.1.21, month/week read-only view), JobForm, JobAssignPanel, WorkBreakdownView}.vue` + composable `useJobFilters` (filter state mgmt). Index toggles calendar ↔ list. Show = detail + assign panel + cancel button + read-only Rozpis prác. Create/Edit = ObjectPicker (required) + type + date + time + optional note + optional contract FK.
  - package.json — added `@fullcalendar/vue3 ^6.1.21`.
  - i18n: `job_status.*`, `job_type.*`, `task_frequency.*`, `schedule.*`, `work_breakdown.*` in `lang/{sk,en,uk}/app.php`. `app.schedule.list_title`, `app.schedule.calendar_title`, `app.schedule.create`, `app.schedule.edit`, `app.schedule.view`, `app.schedule.assign`, `app.schedule.cancel`, `app.job_status.{planned,unassigned,in_progress,completed,unapproved,cancelled}`, `app.job_type.{regular,one_off,special}`, `app.task_frequency.{one_time,weekly_1x,seasonal}`, `app.work_breakdown.title`, `app.work_breakdown.tasks`, etc.
  - Tests: 7 files in `tests/Feature/Schedule/` (JobStatusEnum, WorkBreakdownGeneration, JobService, GenerateScheduledJobs, GenerateScheduledJobsCommand, ScheduledJobPolicy, ScheduledJobController + ObjectControllerTest extended for WorkBreakdownDetailData).
  - RoleTemplatesSeeder — seeded permissions: Vlastník all (ViewSchedule, CreateSchedule, EditSchedule, AssignCleaners), Vedúca (ViewSchedule, CreateSchedule, EditSchedule, AssignCleaners), Upratovačka (ViewSchedule only, own jobs Phase 2), others none by default.
- **Flow (contract sign → breakdown generation):** `POST|PUT /contracts/{id}` → `ContractController::update` calls `ContractService::sign()` → inside `DB::transaction`: `WorkBreakdownService::generateFromContract($contract)` if contract.category=ServiceAgreement + contractable=CleaningObject + quote_id set → creates WorkBreakdown + task per quote item (frequency from item.frequency) → returns breakdown or null (idempotent: existing breakdown for contract → returns existing).
- **Flow (daily job materialization):** `app:generate-scheduled-jobs` daily (routes/console.php) → finds WorkBreakdown where is_active=true + has active contract (withoutGlobalScope for cross-tenant discovery) → dispatches GenerateScheduledJobsJob per breakdown (ShouldBeUnique via breakdown_id) → worker: binds tenant context, materializes ScheduledJob instances per task per frequency within rolling 30d horizon, idempotent via (breakdown_id, task_id, scheduled_date) partial-unique constraint.
- **Flow (job list + calendar):** `GET /jobs` (middleware `feature:schedule`, `#[Authorize('viewAny', ScheduledJob)]`) → props: ObjectOptionData[], membershipOptions, JobIndexFilterData defaults → `Pages/Schedule/Index.vue` → calendar OR list toggle + filters → POST/GET back to controller actions.
- **Flow (job detail + assign):** `GET /jobs/{id}` (middleware `feature:schedule`, `#[Authorize('view', $job)]`) → props: full JobDetailData (status, can {update/assign/cancel}), membershipOptions, workBreakdown (read-only) → `Pages/Schedule/Show.vue` → assign panel (nullable membership picker) → POST `/jobs/{id}/assign` → JobService::assign → redirect with flash.
- **Depends on:** `identity-tenancy` (tenant_id FK, TenantMembership FK, permission gate), `subscription-plans` (route middleware `feature:schedule`), `contracts` (WorkBreakdown.contract_id FK, idempotent generation triggered by Contract::sign), `objects` (ScheduledJob.cleaning_object_id FK, central entity), `quotes` (WorkBreakdown.source_quote_id nullable, items mapped to tasks), `employees` (EmployeeService::deactivate calls JobService::unassignFutureForMembership hook).
- **Depended on by:** (none yet in Phase 1; Phase 2: mobile Upratovačka check-in/out references jobs, invoice links job → cost reconciliation).
- **If you change Core, check:** `WorkBreakdownService::generateFromContract` (Contract.quote_id nullable FK, Contract.category enum, Contract.contractable polymorphic), `ContractService::sign()` (calls generateFromContract inside transaction), `JobService::unassignFutureForMembership` (called by EmployeeService::deactivate, scopes by membership + future scheduled_date), `ScheduledJobController` (8 actions, feature:schedule middleware, Authorize per action), `routes/web.php` (8 jobs.* routes, feature:schedule), routes/console.php (app:generate-scheduled-jobs daily scheduled), `config/scheduling.php` (horizon_days default), GenerateScheduledJobsJob (unique key = breakdown_id, afterCommit = true, setPermissionsTeamId on worker), `RoleTemplatesSeeder` (Vlastník/Vedúca schedule permissions), ObjectController::show (passes workBreakdowns), CleaningObject model (workBreakdowns relation), `lang/{sk,en,uk}/app.php` (job_status.*, job_type.*, task_frequency.*, schedule.*, work_breakdown.* keys), `AppServiceProvider` (policy binding for ScheduledJob).
- **Keywords:** rozvrh, zákazka, rozpis prác, work breakdown, scheduled job, assignment, frequency, recurrence, 30-day horizon, job_status (planned/unassigned/in_progress/completed/unapproved/cancelled), job_type (regular/one_off/special), task_frequency (one_time/weekly/seasonal), FullCalendar, calendar view, list view, assign panel, feature:schedule, Pro/Enterprise.

- **Multi-tenancy global scope** — `App\Concerns\BelongsToTenant` auto-applies `App\Scopes\TenantScope` and auto-fills `tenant_id` on `creating` from `app('current_tenant_id')`. Reach: every domain query. Bypass (jobs/console only): `Model::withoutGlobalScope(TenantScope::class)`. We deliberately did **not** install `stancl/tenancy` — spec mandates the trait+scope approach.
- **Permissions (Spatie teams=tenant_id)** — `config/permission.php`: `teams=true`, `team_foreign_key='tenant_id'`, `'role'=>App\Models\Role::class`. Flat strings via `App\Enums\PermissionEnum` (canonical source of truth — all code references `PermissionEnum::Xxx->value`). BE Policy + Controller `#[Authorize]` gating; FE `Can` component + `useAuthorization().can()` composable read from Pinia store (hydrated from `/api/me`). Reach: every Policy + `#[Authorize]` site + every FE render conditional. **Outside HTTP (seeder/job/console/test) you MUST `setPermissionsTeamId($tenantId)` first** or lookups return "permission not found".
- **Feature gating (entitlement layer)** — `ChecksFeatures` interface + `ConfigFeatureChecker` reads `config/subscription.php` plan matrix. Stacks **on top of** RBAC permissions: plan gates the tenant ("does your tier unlock Invoices?"), Spatie gates the user ("can you view invoices?"). Both must pass (AND logic). BE: Middleware `RequiresTenantFeature` enforces plan entitlements at route level. FE: capabilities store + `useAuthorization().hasFeature()` + `Can` component gate UI. DIP interface allows future `PennantFeatureChecker` or DB-backed adapter without caller changes. Reach: any route gated `->middleware('feature:xxx')` + any FE component checking `hasFeature()`. Quota enforcement (entity count vs. limit) is caller responsibility, checker is stateless.
- **Activitylog** — `LogsActivity` on Role (+ Client/ClientContact). Auth events (Login/Logout/Failed/Lockout/PasswordReset/PasswordChanged/Registered/Verified) are logged asynchronously via `App\Listeners\AuthEventListener` (implements `ShouldQueue`, `#[Tries(3)]`). Writes to `activity_log`. Morph columns `subject_id`/`causer_id` are **strings** to span mixed-PK models (Role bigint, User/Client UUID).
- **MediaLibrary** — `media` table present, `model_id` UUID. No collections wired on domain models yet (reserved for DocumentTemplate / photos).
- **i18n** — SK (default) / EN / UA. `App\Enums\SupportedLanguage`, `lang/{sk,en,uk}/{app,auth,passwords}.php`, `LocaleMiddleware` (order: user.locale → session → cookie → Accept-Language → SK). BE flattens nested keys via `Arr::dot()`; FE `useTranslate()` does flat `t(key)` lookup. Reach: every visible string.
- **FE Forms library** — `resources/js/Components/Forms/` (`FormProvider` + `FormField` + typed inputs: Text/Select/Radio/Number/Textarea/Toggle + `FormActions`, barrel `index.ts`). Contract: `FormProvider` provides form context (`useFormContext`), fields resolve errors via `useFieldError`. Reach: every form drawer/page (`ClientFormDrawer`, `ObjectFormDrawer`, `AddTenantModal`, Register steps, `Invitations/Accept.vue`). Shared also: `ConfirmDialog.vue`, `Pagination.vue`, `useLocalizedDate` composable.
- **Inertia shared props** — `HandleInertiaRequests::share`: `app, auth, tenant{active,available}, can{…}, flash, translations, locale, languages, canResetPassword`. `tenant.available` is a `DataCollection<TenantListItemData>` (typed DTO: id, name, is_active). Typed in `resources/js/types/index.d.ts`, read via `usePageProps()`. Reach: every page.
- **TypeScript transformer** — `php artisan typescript:transform` generates `resources/js/types/generated.d.ts` from `app/Data` + `app/Enums` (`#[TypeScript]`). Never hand-edit. Wired via `App\Providers\TypeScriptTransformerServiceProvider` (no config file in v3).

## Layer contracts

- **HTTP ↔ Service** — Controllers hand a Spatie Data DTO to the service, never an array. Controllers are thin: type-hint DTO param + service in constructor, return `Inertia::render` or `to_route()->with('flash.success', …)`.
- **Service ↔ Model** — `DB::transaction` lives in the Service, NEVER the Controller. Services are `final readonly class XxxService`. Multi-model bootstrap (register) also in Service.
- **BE ↔ FE (Inertia, page render)** — page prop shape = the DTO's generated TS namespace. Shared props also follow the DTO rule: `tenant.available` is a `DataCollection<TenantListItemData>` (not raw arrays). Enums shared via `#[TypeScript]` → `generated.d.ts`. Flash via `flash.success` session key. Inertia provides new-registered user with `justRegistered: boolean` flash to trigger welcome overlay.
- **BE ↔ FE (API: /api/me)** — `GET /api/me` returns `MeData(userId, activeTenantId, permissions: list<string>, features: list<string>)`. Session-auth XHR (cookies). FE capabilities store hydrates from this shape; UI conditionals read from store via `useAuthorization()` composable + `Can` component.
- **BE ↔ FE (API: /api/icos/{ico})** — `GET /api/icos/{ico}` returns `IcoLookupData(ico, name?, vat_status?)` over JSON. Guest-accessible, throttled. FE composable `useIcoLookup` debounces + cancels on unmount.
- **Auth gate** — BE: business models gated by **Policy + `#[Authorize]` per controller method** (rbac-full), not route middleware. FE: `<Can permission feature>` component + `useAuthorization()` composable conditionally render. Both axes (permission + feature) AND together; missing either → deny render / deny route access. Permission lookup is tenant-scoped — relies on `TenantContextMiddleware` having run.
- **Registration (atomic)** — `POST /register` collects all 3 steps client-side (account + company + invites), sends single `RegisterData` DTO, RegistrationService wraps User→Tenant→TenantInterface→TenantMembership→Roles→Invitations in `DB::transaction`. User auto-login + `active_tenant_id` session binding happens post-commit (outside transaction).

## Gotchas

- **`notifications` table is a deliberate exception to multi-tenancy invariant #1** — `DatabaseNotification` is a Laravel framework model: it does NOT use `BelongsToTenant` trait, `HasUuids`, or `TenantScope`. Adding the trait would break `$user->notifications()` / `$user->unreadNotifications()` Eloquent relations. Tenant isolation is enforced manually via three layers: (1) `TenantDatabaseChannel::buildPayload()` injects `tenant_id` on every write; (2) all `NotificationService` queries filter `->where('tenant_id', $tenantId)`; (3) `NotificationPolicy::update` checks `$notification->tenant_id === app('current_tenant_id')`. `Gate::policy(DatabaseNotification::class, NotificationPolicy::class)` registered explicitly in `AppServiceProvider` (no auto-discovery).
- **App-wide morph map in AppServiceProvider** — `Relation::morphMap(['cleaning_object' => CleaningObject::class, 'tenant_membership' => TenantMembership::class])` registered in `AppServiceProvider::boot()` changes the morph type aliases globally. Any polymorphic model now stores these short strings instead of FQCNs. **Impact:** `activity_log.subject_type` (from LogsActivity trait) now stores `'cleaning_object'` instead of `App\Models\CleaningObject`, `'tenant_membership'` instead of `App\Models\TenantMembership`. The Contract model's `contractable_type` column follows the same map. **Important:** if new polymorphic targets are added (e.g., Quote), they MUST be added to the morphMap in `AppServiceProvider`. Existing queries/assertions that compare against FQCNs will break; use `getMorphClass()` helper or the map keys instead. Some tests may need updating to reflect short-string aliases.
- **PDF generation requires Chromium — Docker rebuild on pull** — `spatie/laravel-pdf` `chrome` driver (`chrome-php/chrome`) needs a real Chromium binary. **arm64 constraint:** Google Chrome and Puppeteer's Chrome-for-Testing are x86_64-only on Linux; the Sail image therefore installs Chromium via **Playwright** (`docker/8.5/Dockerfile`), symlinked to `/usr/local/bin/chromium-real`. When pulling this change for the first time, run `docker compose build --no-cache laravel.test`. Without it, `InvoicePdfService::render()` or `ContractPdfService::render()` fails. CI runs tests via mocked `RendersInvoicePdf`/`RendersContractPdf` interfaces, so tests never invoke the real browser. `.env` requires `LARAVEL_PDF_DRIVER=chrome` + `CHROMIUM_PATH=/usr/local/bin/chromium-real` (set in `.env.example`). Verified: real invoice render = 33 KB `%PDF`.
- **Registration atomic scope — no role assignment outside transaction** — `RegistrationService::register()` calls `RoleTemplatesSeeder::seedForTenant()` and assigns Vlastník role *inside* the `DB::transaction`. Post-transaction, the request redirects + auto-logs in (session-only). Do NOT move role assignment outside the transaction; it must happen before user can be queried for permissions in later requests.
- **RoleTemplatesSeeder now static** — refactored from seeder to `static seedForTenant(Tenant)` (called from RegistrationService and add-tenant flow). Outside HTTP (jobs/console), always call `setPermissionsTeamId()` *before* using the seeder or querying roles. Inside RegistrationService transaction, it's already set.
- **TenantInterface uses bigint PK, not UUID** — deliberate exception. Settings models never appear in URLs/DTOs, so bigint is safe. The 1:1 relation is enforced at DB level (unique tenant_id FK). Always create via `TenantInterface::create()` when bootstrapping a tenant; the RegistrationService does this automatically.
- **Email auto-verified on register** — no email-verification flow exists in this SaaS. `RegistrationService::register()` calls `$user->forceFill(['email_verified_at' => now()])->save()` post-creation (email_verified_at is NOT in fillable; forceFill bypasses guards). This is intentional (spec).
- **TenantInvitation partial unique + soft-delete** — migration creates `unique(['tenant_id', 'email'], [], where: "status = 'pending' AND deleted_at IS NULL")`. Pending invites are unique per tenant; soft-deleting one allows re-inviting the same email. Accepting (`InvitationAcceptService`) transitions status Pending→Accepted via `markAccepted()`; multiple Pending rows with same email should never coexist.
- **Soft-delete + partial unique** — `clients (tenant_id, ico) WHERE deleted_at IS NULL AND ico IS NOT NULL`. IČO unique per tenant among *active* clients; resurrected/soft-deleted rows don't block re-use. A plain `unique` would break this. `ClientService` catches the `QueryException` and rethrows as `ValidationException`.
- **Permission team scope outside HTTP** — forgetting `setPermissionsTeamId` in seeders/jobs/tests silently returns empty permissions. Most common multi-tenant bug.
- **`activity_log` morph columns are strings** — not UUIDs, so polymorphic causer/subject works across bigint (Role) + UUID (User/Client/TenantInvitation) models.
- **Spatie permission migration is UUID-patched** — the published `2026_05_03_194335_create_permission_tables.php` was edited: `model_morph_key` + `team_foreign_key` are `uuid()`, but `permissions.id`/`roles.id` stay bigint (Spatie internal, never in URLs/DTOs).
- **Spatie Data v4 ↔ typescript-transformer v3 mismatch** — `DataTypeScriptTransformer` references a removed class. Use `Spatie\LaravelTypeScriptTransformer\LaravelData\Transformers\DataClassTransformer` directly (see the service provider).
- **Activitylog v5 namespace shift** — `…\Models\Concerns\LogsActivity` (not `Traits`), `…\Support\LogOptions`; `dontSubmitEmptyLogs()` → `dontLogEmptyChanges()`.
- **Auth page palette** — `Pages/Auth/Register.vue` + `Login.vue` use `--auth-*` CSS custom properties (brown/amber gradient, not `cleanmaster` DaisyUI theme tokens). Tokenized in `resources/css/app.css` (:62–107). Not the same as `Landing.vue`'s blue/slate or the main app's DaisyUI theme.
- **Landing page palette** — `Pages/Landing.vue` uses raw Tailwind blue/slate, NOT DaisyUI semantic tokens (fixed marketing brand, theme-independent). Don't "fix" it to use the `cleanmaster` theme.
- **`usePageProps()` cast** — Inertia v3 augmentation didn't type-check reliably across imports, so we cast once inside the composable (`usePage().props as unknown as SharedProps`). Revisit if Inertia typing improves.
- **IČO lookup mock is hardcoded** — `IcoLookupService::lookup()` currently checks a static SKMap. Replace with ARES API when ready (flag says `// TODO swap for ARES API`). No unit tests on the mock; API integration tests will be needed post-swap.
- **Subscription plan moved from Tenant to User** — Tenant no longer has a `subscription_plan` column. Plans belong to the **account** (User). Each Tenant has an `owner_id` FK to the User who created it. Feature gating via `$tenant->owner->subscription_plan`, not `$tenant->subscription_plan`. `ConfigFeatureChecker::hasFeature($tenant, $feature)` eagerly loads owner and reads its plan. Tenant quota (max tenants) checked via `ChecksFeatures::canCreateTenant($user)` before POST /tenants. ON DELETE CASCADE when owner is deleted.
- **CleaningObject PHP class name avoids `Object` global** — the model is `CleaningObject` (not `Object`) to avoid collision with PHP's `Object` builtin. The DB table is `objects`, routes use `objects`, permissions use `objects`, only the PHP class name differs. Explicit `Gate::policy(CleaningObject::class, ObjectPolicy::class)` in `AppServiceProvider` required because auto-discovery looks for `CleaningObjectPolicy`, not `ObjectPolicy`.
- **Rule::exists tenant-scope on ObjectStoreData** — `client_id` field includes `Rule::exists('clients', 'id')->where('tenant_id', $tenantId)` in `rules()` method. This closes the cross-tenant leak: a user from tenant A cannot POST `/objects` with a client_id from tenant B, because the Exists rule filters by the resolved `$tenantId` (from `app('current_tenant_id')`). TenantScope applies at query time; DTO validation applies at HTTP boundary.

## Why these versions / decisions

- **PHP 8.5 in Docker/Sail** — host has 8.4; L13 requires `^8.5`. Reproducible toolchain, CI matches local.
- **Vite 7 (not 8)** — Vite 8's Rolldown chokes importing DaisyUI 5 CSS through the Tailwind 4 plugin (`Unknown file extension ".css"`).
- **DaisyUI 5 + Tailwind 4** — `@plugin 'daisyui'` + `@plugin 'daisyui/theme' { name:'cleanmaster'; … }` (OKLCH) in `resources/css/app.css`. No `tailwind.config.js` — CSS-driven via `@theme`/`@source`.
- **Design handoff** — Claude Design JSX prototypes in `docs/design-handoff/`. Source of truth for *visuals only*; re-implement in Vue 3 + Tailwind 4, do **not** copy the React structure or `cm-*` classes. `styles.css` there is reference-only, never imported.

## Build / dev workflow (Sail)

- Whole stack: `./vendor/bin/sail up -d` (laravel.test, pgsql, mailpit, minio). App → http://localhost, Mailpit → :8025, MinIO → :8900.
- Vite HMR: `./vendor/bin/sail pnpm dev` (:5173, `host:'0.0.0.0'`). Prod build: `./vendor/bin/sail pnpm build` → `public/build/{manifest.json, assets/*}`.

## Quality-gate baseline

- **Pint** — passes; canonical L13 preset (strict types, final classes, ordered imports).
- **PHPStan** — level 5 baseline (the published Spatie permission migration carries unavoidable `mixed` from `config()`). Ramp toward max as domain code gains explicit types.
- **vue-tsc** — strict, passes.
- **ESLint** — bans `ref()` for app logic (`no-restricted-syntax`). **Prettier** — `resources/**/*.{ts,vue,css,json}`, 110-char.
- **Lefthook** — pre-commit: pint, phpstan, eslint, prettier, ts-transform; pre-push: vue-tsc.
- **Tests** — PHPUnit, sqlite in-memory; small passing suite on the v0.1 base.

## Migration strategy

Not deployed (`CLAUDE.md › Deployment Status`). Re-run `migrate:fresh --seed` on any schema change. On first production deploy, switch to additive migrations and bump `Last verified`.

## Demo state

`UserSeeder`: User `admin@example.com` / `password` → TenantMembership → Tenant `Demo Cleaning s.r.o.` (IČO 12345678, VAT-payer, subscription plan `Pro`). Sets permission `team_id` to the tenant, runs `RoleTemplatesSeeder` (6 spec roles per tenant), assigns Vlastník to admin.

## Known gaps to close in /feature passes (rough dependency order)

1. **Tenant CRUD** — list/create/edit/switch. Touches AppLayout tenant switcher.
2. **User invite + Profile + change-password.**
3. **Quote** — line items, status enum, PDF, send-to-client, auto work breakdown.
4. **Schedule + Job** — calendar, drag-drop, recurrence from work breakdown.
5. **Absence** — cleaner-reported, scheduling-impact notification.
6. **DocumentTemplate** — upload/download per type enum (MediaLibrary).
7. **Audit log read UI** — surface existing Spatie ActivityLog rows.
8. **Subscription / Plan enforcement** — **gating engine done** (config + enums + middleware); entity-limit enforcement at write-time (quota checks in service layer) + UI still pending.
9. **Mobile + customer portal scaffolding** (Phase 2).

## Verification status

- Last full scan: 2026-06-05 (`/init` full re-init — schema + routes via Boost MCP, source grep)
- Last delta: 2026-06-22 (Employees module: TenantMembership extended (nullable first_name/last_name/phone/position), LogsActivity, employmentContract morphMany relation. EmployeeService (paginate/create/update/deactivate with escalation guards + quota). TenantMembershipPolicy (rbac-full). EmployeeController (7 routes: index/create/store/show/edit/update + POST deactivate), feature-gated `feature:employees`. New PermissionEnum: ViewEmployees/CreateEmployees/EditEmployees/AssignEmployees/DeleteEmployees. RoleTemplatesSeeder: Vedúca (view+assign), Vlastník (all). DTOs: EmployeeIndexFilterData/ListItemData/DetailData/UpsertData. FE: Pages/Employees/{Index,Create,Edit,Show}, components (EmployeeForm, EmployeeFiltersBar, PermissionCheckboxGroups, EmployeeStatusBadge), composable useEmployeeFilters. AppLayout nav "Zamestnanci". Tests: 3 files in tests/Feature/Employees/ (EmployeeServiceTest, EmployeePolicyTest, EmployeeControllerTest) covering escalation guards, quota, permission intersect. Also: updated business.md (manage employees use-case + permissions section), technical.md (employees domain block + Depends/Depended/If-you-change-Core checks), CLAUDE.md (Employees to modules list + removed from out-of-scope). Notifications module note: InvitationCreated now dispatched also by EmployeeService::create for new users.)
- Open `TODO verify`: 1 (IČO lookup service mock — ARES API swap pending)
- Reference inventory: `.claude/inventory.md` (not generated; opt-in via `/spec-sync --full --with-inventory`)
